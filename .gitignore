# dependencies
/node_modules
agents/node_modules
/.pnp
.pnp.js

# crawler cookies (sensitive — Naver login session)
agents/cafe/storage-state.json
agents/cafe/storage-state-jisik.json

# naver blog poster (sensitive — cookies, queue state, temp files)
agents/naver-blog/blog-storage-state.json
agents/naver-blog/queue.json
agents/naver-blog/.blog-halted
agents/naver-blog/.blog-posting.lock
agents/naver-blog/temp/
agents/naver-blog/session-backups/
agents/naver-blog/dry-run-screenshots/

# testing
/coverage
/test-results/
/playwright-report/
/blob-report/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (절대 커밋 금지 — CSO 정책)
.env
.env.*
!.env.example
# 추가 보안: 실수로 env 파일이 다른 이름으로 저장되는 것 방지
*.env
credentials.*
secrets.*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea/
.vscode/settings.json

# Prisma
/prisma/migrations/dev/

# OS
Thumbs.db

# Misc
antigravity screenshot.png

/src/generated/prisma
.env.vercel
.env.vercel-check
supabase/.temp/

# Crawler logs
/logs/

# Playwright QA — auth storageState (인증 쿠키 포함, 커밋 금지)
e2e/.auth/
# Playwright 결과 JSON
playwright-report/results.json
agents/core/today-brief.json

# Playwright 스크린샷 (테스트 산출물)
e2e/screenshots/

# Design assets — 에이전트 생성 결과물 (커밋 제외, README 참고)
assets/generated/

# Local debug scripts
check-data.mjs
# 일회성 진단·측정 스크립트 (언더스코어 prefix) — 로컬 보존, git 추적 제외
agents/scripts/_*.ts

# 매거진 로컬 러너 일일 결과 파일 (gitignore — 로컬 전용)
agents/cafe/.magazine-daily-*.json

# Local plugin state (bkit, OMC)
.bkit/
.omc/
agents/.omc/

# QA 결과 파일 (생성 산출물 — 커밋 제외)
visual-qa-result.json

# 로컬 광고 소재 (대용량 이미지/영상 — git 불가)
assets/campaigns/
assets/hero_banner/
assets/review/

# 네이버 세션 백업 및 차단 플래그 (쿠키 포함 — 절대 커밋 금지)
agents/cafe/session-backups/
agents/cafe/.session-halted

# MCP 설정 (토큰 포함 — 절대 커밋 금지)
.mcp.json

# Playwright MCP 로그
.playwright-mcp/

# 로컬 툴 상태 / 락 파일 (절대 커밋 금지)
.claude/scheduled_tasks.lock
.serena/

# 스크린샷 / 스토어 자산 (대용량 — git 불가)
play-store-screenshots/
playwright-screenshots/
assets/

# 임시 쿼리 / 분석 스크립트 (루트 레벨)
agents/query*.js
agents/query*.cjs
agents/query*.mjs
agents/query*.ts
query*.mjs
query_logs.ts
analyze-posts.ts
test-query.ts

# 백필 결과 CSV (일회성 산출물)
agents/scripts/backfill-*.csv
