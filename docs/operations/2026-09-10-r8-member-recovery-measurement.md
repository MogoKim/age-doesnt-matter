# R8 회원 소생 측정 — AS-IS 대조와 측정 정의 (2026-09-10)

> 목적: 실회원이 **① 가입 ② 첫 작성 ③ 답글 ④ 재방문** 중 어디서 끊기는지 어드민에서 본다.
> 화면 `/admin/member-recovery` · 쿼리 `src/lib/queries/admin/admin.member-recovery.ts`
> ⚠️ 이 배치는 **측정만** 한다. 가짜 댓글·좋아요·회원 행세 자동화는 만들지 않았다.

## 1. AS-IS 전수 대조 — 이미 있는 것은 다시 만들지 않았다

| 구간 | 기존 자산 | 판정 | 이번 조치 |
|---|---|---|---|
| 방문 세션·채널별 가입 | `admin.insights.ts` `channels` | 채널별 가입률만 있고 **단계 연결이 없다** | 퍼널로 연결 |
| 가입 후 활동 | `admin.insights.ts` `activation` | `User.postCount > 0` **누적 스냅샷** — 코호트·기간·소요시간 없음 | 가입일 코호트로 재정의 |
| 회원 D1~D30 재방문 | `admin.retention.ts` `getRetentionQuadrants()` | **이미 구현돼 있다**(가입 코호트 · `providerId` 숫자 · `role≠ADMIN` · 성숙 코호트 분모) | **재구현 안 함 — 호출해서 붙임** |
| 댓글 → 다른 실회원 답글 | 없음 | 신규 | 구현 |
| 일일 KPI | `DailyKpiSnapshot` | `retention` JSON 은 **비회원 EventLog 기준** | 섞지 않음 |

### 이벤트 계측 현황 (전수)

| 단계 | 이벤트 | 상태 |
|---|---|---|
| 비회원 방문 | `page_view` (가입 전 방문만 — §2) | ✅ 수집 |
| 가입 유도 노출 | `signup_banner_shown` | ✅ 수집 |
| **배너 CTA 반응** | `signup_banner_clicked` **전체 `cta_type`** (`kakao_oauth` · `app_install` · `external_browser`) | ✅ 수집 · `cta_type` 별 분해 가능 |
| 가입 완료 | `sign_up` (이벤트) / `User.createdAt` (사실) | ⚠️ 두 값은 다르다 — 완전성은 ID 대조로만 판정(§3) |

🔴 **사이트 전체 `kakao_button_click` 은 배너 반응이 아니다.** 로그인 화면·게스트 댓글 카드 등
여러 표면에서 발생하므로 **배너 전환에 귀속하지 않고 별도 참고값**으로만 둔다.
과거에 `kakao_button_click ∪ signup_banner_clicked{cta_type:'kakao_oauth'}` 를 "카카오 로그인 시작"
한 단계로 묶었던 정의는 **철회했다** — 배너가 하지 않은 일을 배너 전환율로 읽게 만들었다.

⚠️ 노출 이벤트(`signup_banner_shown`)에는 **어떤 CTA 를 보여줬는지 정보가 없다.**
그래서 `cta_type` 별 **노출 분모는 복원할 수 없고**, 분해는 **클릭 쪽에만** 있다.

## 2. 판정 기준 (전 구간 공통)

- **실회원** = `providerId` 가 순수 숫자(`^\d+$`, 카카오 user ID) **AND** `role !== 'ADMIN'`
  접두어(`bot-`·`seed`·`curator-`) 목록 방식은 7건 어긋난다 — 숫자 판정이 SSoT다.
- 이벤트는 `isBot = false` 만 센다.
- **내부 세션 제외**: `/admin` 경로를 본 세션 또는 `botType='founder'`. 어드민 계정도 분모·분자 양쪽에서 뺀다.
- **분모와 분자를 항상 함께 표시**한다. 비율만 있는 칸은 만들지 않는다.
- **성숙하지 않은 대상은 분모에서 뺀다** — 가입 24h 미경과자, 작성 24h 미경과 댓글. 이들은 "안 했다"가 아니라 "판정할 시간이 없었다"라서 별도 칸으로 보여준다.
- **분모가 0이면 `rate = null`(판정 불가)** 로 두고 화면에도 "판정 불가"로 그린다. **0% 로 그리지 않는다.**

## 3. 알려진 계측 한계 — 숫자보다 먼저 읽는다

| 키 | 등급 | 내용 |
|---|---|---|
| `login_start_rate_limit` | WARN | `kakao_button_click` 이 `api/events` 의 rate limit 면제 목록(`CONVERSION_EVENTS`)에 **없다**. `page_view` 와 같은 버킷(`event:ip`, max 30)이라 429 로 조용히 유실될 수 있다 → **로그인 시작은 하한값**이다. 화면에 `≥` 로 표시한다. |
| `oauth_session_split` | WARN | 카카오 OAuth 는 외부 도메인을 왕복한다. 복귀 시 세션 식별자가 바뀌면 "로그인 시작 → 가입 완료"가 같은 세션으로 안 이어져 전환율이 **실제보다 낮게** 나온다. |
| `exposure_cta_unknown` | WARN | 노출 분모(`signup_banner_shown`)에 CTA 종류 정보가 없다 → **CTA별 전환율은 과거 데이터로 복원 불가**. `cta_type` 분해는 클릭 쪽에만 있다. |
| `banner_vs_sitewide_click` | OK | 배너 반응은 `signup_banner_clicked` 로만 센다. 사이트 전체 `kakao_button_click` 은 **배너 전환에 귀속하지 않고** 참고값으로 둔다. |

### 가입 수집 완전성의 판정 — **ID 대조 하나뿐이다**

**유일한 판정 기준은 `sign_up` 이벤트의 `userId` 와 신규 실회원 코호트 ID 의 대조**다.
`matchedMembers / actualNewMembers` 로 계산하고, `missingMembers`(연결 안 된 회원)와
`unlinkableEvents`(`userId` 가 없거나 코호트 밖인 이벤트)를 분리해 함께 적는다.

건수 비교(`sign_up` 이벤트 수 vs 신규 회원 수)는 **판정 근거가 아니다** —
서로 다른 사람의 "3건 · 3명"이 100% 로 보이기 때문이다.
그래서 건수 기반 `signup_cross_check` 항목은 **제거했다**(코드·화면·문서 모두).

이 한계는 **이번 배치에서 고치지 않았다.** 계측 코드를 바꾸면 측정 대상 자체가 바뀌어 이전 데이터와 비교가 끊긴다.
`CONVERSION_EVENTS` 에 `kakao_button_click` 을 넣는 것, 노출 이벤트에 `cta_type` 을 동봉하는 것은
**후속 판단 대상**이다(§7-A).

## 4. 4단계를 다시 계산하지 않은 이유

`admin.retention.ts` 는 **90일 창**을 쓴다. D7 은 가입 후 7일이 지나야 판정할 수 있어서,
7일 창에서 D7 을 재면 성숙 코호트가 거의 0이 되고 **분모 0이 "재방문 0%"로 오독**된다.
그래서 이 화면의 7·30일 토글은 **1~3단계에만** 적용하고, 4단계는 기존 지표를 출처·창 길이와 함께 그대로 싣는다.

**비회원 리텐션은 이 화면에 싣지 않는다.** 같은 함수가 돌려주지만 코호트 정의가 다르다
(비회원 = 첫 `page_view` 일 기준). 섞으면 "회원 재방문"으로 오독된다. 회귀 테스트로 고정했다.

## 5. 개인정보

화면과 쿼리는 **건수·비율·상태만** 다룬다. 닉네임·본문·댓글·`providerId`·IP 를 조회하지도 출력하지도 않는다.

## 6. production 실측 기준선 (2026-09-11 · **3차 보정판**, read-only)

> 🔴 **초판·1차·2차 수치를 모두 철회한다.**
> 초판은 `page_view` 행 수를 "세션"이라 불렀고, 1차는 정렬 없는 페이징으로 중복이 섞였으며,
> 2차는 **사이트 전체 `kakao_button_click` 을 배너 전환에 귀속**했다.
> 아래는 keyset(`order=id.asc` + `id=gt.<cursor>`) + exact count 대조로 무결성을 확인한 값이다.

### ① 전체 배너 노출 방문자 / ② 전체 배너 CTA 반응 방문자

| 지표 | 최근 7일 | 최근 30일 |
|---|---:|---:|
| 비회원 방문자 | 232 | 802 |
| **① 배너 노출 방문자** | **31** | **135** |
| **② 배너 CTA 반응 방문자(전체)** | **0** | **0** |
| 비회원 방문 → 노출 | 31/232 · 13.4% | 97/802 · **12.1%** |
| 노출 → 배너 CTA 반응 | 0/31 · **0%** | 0/135 · **0%** |

### ③ `cta_type` 별 클릭 방문자

| `cta_type` | 최근 7일 | 최근 30일 |
|---|---:|---:|
| `kakao_oauth` | 0 | 0 |
| `app_install` | 0 | 0 |
| `external_browser` | 0 | 0 |
| 기타·미상 | 0 | 0 |

**교차 확인(exact count, 봇 포함 전체 행)**: `signup_banner_clicked` 는 **7일 0 · 30일 0 · 90일 26 · 전체 26** 이다.
즉 **배너 클릭은 최근 30일 동안 한 건도 없었고**, 기록된 26건은 전부 90일 창 안쪽의 과거분이다.

> ⚠️ **2차 보고 정정**: 2차에서 「노출 → 로그인 시작 4명(2.7%)」이라고 적은 그 4명은
> **배너 클릭이 아니라 사이트 전체 `kakao_button_click`** 이었다. 배너 CTA 전환율로 읽으면 안 된다.

**[참고 · 배너 아님] 사이트 전체 `kakao_button_click` 방문자**: 7일 **3** · 30일 **15**.
로그인 화면·게스트 댓글 카드 등 여러 표면에서 발생하며 **배너 전환에 귀속하지 않는다.**
이 이벤트는 rate limit 면제 목록에 없어 유실 가능성이 있다(§3).

### 계측 일관성 — 적격 vs 노출 (전환율 아님)

| | 최근 7일 | 최근 30일 |
|---|---:|---:|
| 적격 방문자 | 31 | 92 |
| 노출 방문자 | 31 | 135 |
| 둘 다 있음(정상) | 31 | 92 |
| 적격만 있음 | 0 | 0 |
| **노출만 있음** | 0 | **43** |

두 이벤트는 `SignupPromptBanner.tryFire` 에서 **연속 동기 호출**되는 fire-and-forget POST 라
서버 기록 순서가 경쟁 조건이다. **전환율을 만들지 않는다.**
30일에서 **노출만 있는 방문자 43명**은 전환 실패가 아니라 **`signup_banner_eligible` 전송 유실** 신호다
(행 기준으로도 shown 178 vs eligible 131).

### ④ ID 대조 가입 완전성

| 창 | 실제 신규 실회원 | ID 로 연결된 회원 | 연결 안 된 회원 | 연결 불가 이벤트 | 완전성 |
|---|---:|---:|---:|---:|---:|
| 7일 | 0 | 0 | 0 | 0 | 판정 불가(분모 0) |
| 30일 | 3 | **3** | 0 | 0 | **100%** |

### 이 수치로 다시 판정한 것

**「가입 유도 노출 → 배너 CTA 반응」이 직접 로그인 기록이 낮은 후보**다. **확정하지 않는다** —
노출 분모에 CTA 종류 정보가 없고, 배너 밖 가입 경로가 실재하며, 표본이 작다.

- **배너는 노출되지만 클릭이 0이다** — 30일 135명이 봤고 어떤 CTA 도 눌리지 않았다.
  다만 **노출 분모에 CTA 종류 정보가 없어** "카카오 CTA 를 본 사람 중 몇 %" 는 만들 수 없다.
- **가입 3명은 배너를 거치지 않았다.** 같은 30일 배너 클릭 0, 사이트 전체 카카오 클릭 15, 실제 가입 3명이다.
  가입 경로는 배너 밖(로그인 화면 등)에 있다.
- **가입 단계 자체는 문제로 볼 근거가 없다** — ID 대조 완전성 100%, 표본 3명.
- **7일 창은 신규 가입 0** 이라 2단계·완전성 분모가 없어 **판정 불가**다(0% 아님).

내부 방문자 제외는 30일 기준 **행 883 · 고유 방문자 2명**(창업자 세션)이다.

## 6-B. 공유 지표 보정 — `admin.retention.ts` 에 `isBot=false` 추가

회원 리텐션은 `EventLog` 의 `page_view`·`login` 으로 "그 날 활동했다"를 판정하는데,
**회원 id 가 붙었지만 봇으로 표시된 이벤트**(`botType='e2e-test'`·`'founder'` 등)에 필터가 없었다.
그대로 두면 그 날짜가 활성으로 잡혀 **D1·D7 이 실제보다 높게** 나온다. 비회원 쪽은 이미 걸러지고 있었다.

- `where` 에 `isBot: false` 추가, 캐시 키를 `v3 → v4` 로 올려 옛 값이 그대로 나오지 않게 했다.
- 이 지표는 **R8 화면이 그대로 재사용**하므로 여기가 틀리면 두 화면이 함께 틀린다.
  회귀 테스트 `src/__tests__/retention-bot-exclusion.test.ts` 로 고정했다(필터 존재 + 동작 + 어드민 제외).

## 7. 후속 (이번 PR 범위 아님)

### 7-A. 신규 계측 제안 — **이번 PR 에서 이벤트 생성 코드는 바꾸지 않았다**

지금 데이터로는 답할 수 없는 것이 있고, 그 대부분은 **계측을 늘려야만** 풀린다.
계측을 바꾸면 **이전 기간과 비교가 끊기므로** 적용 시점을 기록하고 전후를 나눠 읽어야 한다.

| # | 제안 | 왜 필요한가 |
|---|---|---|
| 1 | `signup_banner_shown` 에 **`cta_type`(또는 variant) 동봉** | 지금 노출 분모에는 어떤 CTA 를 보여줬는지 정보가 없다. "카카오 CTA 를 본 사람 중 몇 %가 눌렀나"를 만들 수 없다 |
| 2 | `kakao_button_click` 에 **`from` 을 표면 단위로 정규화** + `CONVERSION_EVENTS` 면제 | 현재 배너 밖 카카오 클릭이 한 덩어리이고 rate limit 유실 가능성이 있다. 가입 경로가 배너 밖에 있는데 그 경로를 못 가른다 |
| 3 | `signup_banner_eligible` 전송 유실 원인 확인 | 30일 기준 **노출만 있는 방문자 43명** — 두 이벤트가 같은 시점에 나가는데 한쪽만 남는다 |



1. **어드민 첫 화면 링크** — `/admin` 진입 화면에 `/admin/member-recovery` 링크 추가. 그 파일은 다른 작업 흔적이 있어 이번 PR 에서 건드리지 않았다.
2. **`kakao_button_click` rate limit 면제** — 넣으면 로그인 시작이 하한값에서 실측값이 된다. 다만 계측 변경이라 **이전 기간과 비교가 끊긴다.** 적용 시점을 문서에 남기고 그 전후를 나눠 읽어야 한다.
3. **`docs/features/REGISTRY.md` 등록** — 레지스트리는 2026-05-14 이후 갱신이 멈췄고 "수동으로 편집하지 마라" 배너가 붙어 있다. R4/R5 재작성 때 이 기능을 함께 넣는다.
