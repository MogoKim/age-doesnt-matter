# REWRITE_BRAND_COPY 59건 — SEO 문구 정정안 (출처 분리판)

> **정정안(제안)이다. 이 배치에서 DB 를 바꾸지 않았다.**
> production write · Post 변경 · 배포 · merge **0건**.
>
> 기준 main `137cb0a5` · 대상 재조회 **2026-09-11** (59/59 최신 production 값 대조, 불일치 **0**)
> 선행: [2차 분류](./2026-09-11-review-tier2.md) · 전건 표: [`data/2026-09-11-seo-brand-copy-rewrite.csv`](./data/2026-09-11-seo-brand-copy-rewrite.csv) (**59행 · 23열**)

---

## 0. 정책 — 사실 인용은 보존하고, 우나어 카피만 고친다

| 보존한다 (사실 인용 예외) | 고친다 |
|---|---|
| 회사·기관의 **정확한 고유명** | 우나어가 덧붙인 **일반 마케팅·SEO 문구** |
| **정부 공식 제도명** (`노인일자리사업`) | |
| 공고 **공식 직함·기관 유형** | |
| 원문 `title`·`content` (🔒 불변) | |

두 가지를 절대 하지 않았다.

1. **고유명과 비슷한 가짜 일반명을 만들지 않았다.** 대체어를 확정할 수 없으면 고치지 않고 사람 검토로 넘겼다(§4).
2. **회사명에 같은 단어가 있다는 이유만으로 일반 SEO 문구를 회사명 인용으로 추정하지 않았다.** 근거는 §2.

---

## 1. 결과 요약

| | 건수 |
|---|---:|
| 대상 행 | **59** |
| **수정 대상** — `seoDescription` | **51행 · 51필드** (전부 JOB) |
| 수정 대상 없음 — 보존만 | **8행** |
| 금지 표현 출현 총계 | **93** |
| 그중 **BRAND_COPY** (정정 대상) | **74** |
| 그중 **보존**(SOURCE_TITLE_COPY·OFFICIAL_*) | **19** |
| 정정 후 **BRAND_COPY 잔존** | **6 출현 / 5행** — 전건 사람 검토 대기 |
| 사실 보존 검증 불일치 | **0** (51필드 × 10축) |

**`seoTitle` 수정은 0건이다.** 59건 전부 `seoTitle` 의 금지 표현이 원문 공고 제목 복사 구간 안에
있거나(SOURCE_TITLE_COPY) 공식 제도명이었다. 원문을 바꾸지 않는다는 원칙상 손대지 않았다.

### `rewriteDecision` 4분류 (CSV 열)

| 값 | 건수 | 뜻 |
|---|---:|---|
| `REWRITE_BRAND_COPY` | **51** | 우나어 카피 → 정정안 있음 |
| `BRAND_COPY_HOLD_REVIEW` | **4** | 우나어 카피지만 **대체어 확정 불가** → 보류(§4) |
| `OFFICIAL_NAME_ONLY_REVIEW` | **3** | 정부 공식 제도명만 등장 → 전건 보존 |
| `SOURCE_TITLE_ONLY_REVIEW` | **1** | 원문 제목 복사 구간에만 등장 → 수정 필드 없음 |
| **합계** | **59** | |

---

## 2. `termProvenance` — 금지 표현 출현 93건의 출처 분리

각 **출현 위치마다** 출처를 판정했다. 행 단위가 아니라 문자 위치 단위다.

| `termProvenance` | 출현 | 판정 규칙 | 조치 |
|---|---:|---|---|
| `BRAND_COPY` | **74** | 아래 어디에도 해당하지 않음 | **정정** |
| `SOURCE_TITLE_COPY` | **10** | `seoTitle` 이 원문 `title` 로 시작할 때, **그 접두 구간 안에** 위치 | 보존 |
| `OFFICIAL_PROGRAM_NAME` | **6** | `노인일자리 및 사회활동 지원사업` / `노인일자리사업` / `노인일자리` 안에 위치 | 보존 |
| `OFFICIAL_JOB_TITLE` | **3** | JOB이고, 금지어를 품은 **복합 토큰**이 원문 제목의 토큰 집합에 **정확히** 존재 | 보존 |
| `PROPER_NOUN_EXACT` | **0** | 회사 상호 **전체 문자열**이 그대로 인용된 구간 안에 위치 | 보존 |
| | **93** | | |

### 2-A. `PROPER_NOUN_EXACT` 가 0인 근거 — 실측

회사명 겹침을 고유명 인용으로 **추정하지 않았다**. 실제로 세어 보면 겹치지 않는다.

| 측정 | 건수 |
|---|---:|
| `JobDetail.company` 에 금지 표현이 들어 있는 행 | **24** |
| `seoTitle`/`seoDescription` 에 **회사 상호 전체**가 그대로 등장하는 행 | **9** |
| **두 집합의 교집합** | **0** |

즉 상호에 금지어가 있는 24건은 그 상호를 SEO 문구에 인용하지 않았고,
상호를 인용한 9건의 상호에는 금지어가 없다. **그 24건의 출현은 전부 일반 SEO 키워드(BRAND_COPY)다.**

### 2-B. 상호 부분 겹침은 유형별로 갈랐다

토큰이 상호의 **부분 문자열**일 때는 그 토큰의 성격으로 판단했다.

| 토큰 성격 | 처리 | 근거 |
|---|---|---|
| **법정 시설 유형명** — `노인복지센터`·`노인요양원`·`노인요양센터`·`노인주간보호센터`·`재가노인복지` | 일반 키워드로 보고 **정정** | 노인복지법·장기요양보험 체계의 유형명이라 어느 업체나 쓴다. `명촌노인복지센터` 에서 고유 식별자는 `명촌` 이다 |
| **업체가 만든 브랜드 표현** — `실버케어`·`실버마을`·`시니어케어` | **보류**(§4) | 법정 유형명이 아니다. 상호에 들어 있으면 그 업체를 가리킬 개연성이 있어 기계적으로 확정할 수 없다 |

### 2-C. 원문 `title` 에 금지 표현이 있는 12건은 원문을 바꾸지 않았다

| 보드 | 건수 | 예 |
|---|---:|---|
| JOB | **10** | `노인주간보호 운전원` · `노인요양원 사회복지사` · `노인돌봄 생활지원사` |
| MAGAZINE | **2** | `노인일자리, 몇 살부터 신청할까` 등 |

---

## 3. 치환 원칙 — 확인된 사실만 쓰고 조어를 만들지 않는다

단어 대 단어가 아니라 **구절 단위**로 매칭했다(적용된 치환 쌍 **41종**).
치환어는 **그 공고에서 확인된 사실로만** 결정했다.

| 게이트 | 확인될 때 | 확인되지 않을 때 |
|---|---|---|
| 방문·재가 여부 | `방문 돌봄` · `1인 가구 방문 돌봄` | `돌봄` (방문 여부를 새로 주장하지 않는다) |
| 시설 유형 | `요양원 돌봄` | `돌봄` |
| 성별 | `여성 이용자` · `남성 이용자 담당` | 성별어를 쓰지 않는다 |
| 장기요양 등급 | `4등급 이용자 돌봄` (등급 유지) | 등급을 쓰지 않는다 |

**시설 유형은 상위 일반명으로만 올린다.** `노인복지센터 → 복지센터`(재가·주간·입소를 새로 주장하지 않음),
`노인요양원 → 요양원`, `재가노인복지 → 재가복지`.

### 3-A. 마스터가 지목한 7개 표현 — 전건 재검수 결과

| 지목 표현 | 처리 |
|---|---|
| `정부 공공일자리 사업` | **사용하지 않음**(0건). 공식 제도명 `노인일자리사업` 은 그대로 보존한다 |
| `돌봄복지` | **사용하지 않음**(0건) |
| `요양케어` | **철회**. `요양+케어` 중복 조어다 → 시설 유형이 확인된 1건만 `요양원 돌봄` |
| `재가 케어` | **사용하지 않음**(0건). `케어` 접미 조어를 전부 없앴다 |
| `독거 가구` | **사용하지 않음**(0건) → `1인 가구` |
| `이용자를 모시는` | **사용하지 않음**(0건) → `이용자를 돕는` |
| `노후 전용 주거단지` | **철회**(0건). 실재하지 않는 조어라 §4로 넘겼다 |

### 3-B. 문맥 재검수로 추가 교정한 것

| 문제 | 교정 |
|---|---|
| 방문 여부 **미확인** 공고에 `방문요양` 을 씀 (1건) | `어르신 요양보호사 취업 → 요양보호사 취업` (금지어만 제거) |
| 같은 문장에 `중장년` 이 연달아 중복 (1건) | 앞쪽을 삭제. **연령을 새로 주장하지 않는다**(`50·60대` 같은 대체 삽입 철회) |
| `50대 여성 시니어 일자리` → `50대 여성 중장년 일자리` 로 중복 (1건) | `50대 여성 일자리` |
| 같은 문구가 두 번 반복 (2건) | `1인 가구 이용자 돌봄` / `1인 가구 방문 돌봄` 으로 분리 |
| `노인 케어 → 재가 돌봄` 이 앞 구절과 중복 (1건) | `방문 돌봄` |

---

## 4. ⚠️ 치환 보류 5행 — 사람 검토 필요 (`needsHumanReview = true`)

**가짜 일반명을 만들지 않기 위해 고치지 않았다.** CSV `residualBrandCopy` · `reviewReason` 열로 식별된다.

| # | 보드 | 잔존 토큰 | 보류 사유 |
|---|---|---|---|
| 1 | MAGAZINE | `실버타운` | 금지어 없는 **일반 명칭이 실재하지 않는다**. 법정 용어는 `노인복지주택` 이라 `노인` 이 다시 들어간다. `노후 전용 주거단지` 같은 조어는 만들지 않았다 |
| 2 | JOB | `실버케어` | 상호 `삼성재활실버케어` 의 부분 문자열 |
| 3 | JOB | `실버케어` | 상호 `포그니 재활실버케어` 의 부분 문자열 |
| 4 | JOB | `실버마을` | 상호 `사회복지법인삼농복지재단삼농실버마을` 의 부분 문자열 |
| 5 | JOB | `시니어케어` | 상호 `브라보 시니어케어 강남센터` 의 부분 문자열 |

2~5번은 **상호를 가리키는 축약인지 일반 SEO 키워드인지 문서만으로 확정할 수 없다.**
확정하려면 각 업체가 실제로 그 표현을 자기 이름으로 쓰는지 확인해야 한다 — 창업자 판단 영역이다.

1번(`실버타운`)은 성격이 다르다. 상호 문제가 아니라 **대체어 자체가 없다.**
선택지는 ⓐ `노인복지주택`(법정 용어, `노인` 재진입) ⓑ 문장 재작성 ⓒ 현행 유지 — 셋 다 창업자 결정이 필요하다.

---

## 5. 사실 보존 검증 — 10축 전수

수정 51필드 전부에 대해 수정 전/후를 비교했다. **불일치 0.**

| 축 | 검증 방법 | 결과 |
|---|---|---|
| 숫자(급여·시급·인원) | 숫자 토큰 다중집합 동일 | ✅ 0 |
| 장기요양 등급 | `N등급` 토큰 동일 | ✅ 0 |
| 성별 | 여성/남성 표기 집합 동일 | ✅ 0 |
| 근무 형태 | `파트타임`·`오전`·`야간없음`·`주N회`·`교대` 등 동일 | ✅ 0 |
| 회사 | `JobDetail.company` 가 원문에 있었으면 수정안에도 존재 | ✅ 0 |
| 지역 | `location`/`region` 동일 | ✅ 0 |
| 급여 | `salary` 동일 | ✅ 0 |
| 직무 | `jobType` 동일 | ✅ 0 |
| 근무시간·근무일 | `workHours`·`workDays` 동일 | ✅ 0 |
| **시설·서비스 유형** | 원문에 없던 유형어를 새로 만들지 않았는가 | ✅ **신규 6건 전부 사실 근거 있음** |

**유형어 신규 6건**은 전부 `방문` 이고, 6건 모두 해당 공고가 방문·재가 서비스임이
원문 제목·본문·상호로 확인된 행이다(`isVisit = true`). 근거 없는 유형 주장은 **0**이다.

---

## 6. 정정 후 production 렌더 기준 금지 표현 잔존

`generateMetadata` 규칙(JOB title = `seoTitle ?? "${title} — ${company} 채용"` ·
JOB description = `seoDescription ?? 본문 첫 100자 + location + salary`)으로 재구성해 측정했다.

| 구분 | 행 |
|---|---:|
| **보존 근거 있음** — 원문 제목 인용 · 공식 제도명 · 공식 직함 | **13** |
| **BRAND_COPY 보류** — 사람 검토 대기(§4) | **5** |
| **합계** | **18** |

> ⚠️ **잔존 0은 이 배치의 목표가 아니다.** 사실 인용을 보존하기로 한 정책의 필연적 결과다.
> 목표는 **BRAND_COPY 잔존 0** 이고, 현재 **5행이 판단 보류 상태로 남아 있다.**

---

## 7. 🔒 적용 사전조건 — 불일치 행은 자동 중단

이 CSV 를 실제 DB 에 적용할 때 **행 단위로 다음을 반드시 먼저 확인한다.**

```
전제: rewriteDecision == 'REWRITE_BRAND_COPY' 인 행만 적용 대상이다.

행마다:
  live = SELECT "seoTitle","seoDescription" FROM "Post" WHERE id = <csv.id>
  IF live."seoTitle"       <> csv.currentSeoTitle        → 이 행 SKIP, 중단 로그
  IF live."seoDescription" <> csv.currentSeoDescription  → 이 행 SKIP, 중단 로그
  ELSE UPDATE "Post" SET "seoDescription" = csv.proposedSeoDescription WHERE id = <csv.id>
```

- 비교는 **정확 일치**다. 공백·개행 정규화 없이 문자열 그대로 비교한다.
- `currentSeoTitleSha256_12` · `currentSeoDescriptionSha256_12` 열로 눈으로도 대조할 수 있다
  (SHA-256 앞 12자, 측정 시각 `liveValueVerifiedAt = 2026-09-11`).
- **불일치 행은 건너뛰고 중단 로그에 남긴다.** 값이 달라졌다는 것은 그 사이 누군가 고쳤거나
  재수집이 덮어썼다는 뜻이므로, 이 정정안의 전제가 깨진 것이다. 덮어쓰면 안 된다.
- `needsHumanReview = true` 5행과 `rewriteDecision != REWRITE_BRAND_COPY` 8행은 **적용 대상이 아니다.**
- 적용은 **DB write 이므로 COO 에이전트 경로 + 창업자 승인**이 있어야 한다. 이 문서는 승인 요청이 아니다.

---

## 8. 이번 배치에서 하지 않은 것

- production DB write · `Post` 상태 변경 · 배포 · merge — **0건**
- 원문 `title` · `content` 변경 — **0건**
- `JobDetail` (회사·지역·급여·근무조건) 변경 — **0건**
- 개인정보·계정 식별값·본문 전문의 CSV/문서 기록 — **0건**
