# 우나어 운영 정본 (MASTER OPERATING SYSTEM)

> **이 문서가 우나어 운영의 단일 기준점이다.** 전략·현재 상태·남은 작업·다음 배치를 한 곳에서 본다.
>
> 최근 갱신 **2026-09-11** · 현재 기준 **main `325ff74e` · production `2026.09.11-325ff74`**
> (최초 작성 2026-09-10 `18792f44` 기준 — **역사 기준**이다)
> 이번 갱신 반영: **#463 R8 회원 소생 측정 배포** · **#464 R8-v2 배너 CTA 계측 배포** ·
> **#465 글상세 React #418 수정 배포**(production 40/40 검증) · 폐기 후 DB 재실측 ·
> 공개 콘텐츠 인벤토리·1차 분류(846건 전수)
> 통합 대상: Rescue 마스터플랜 · 현재 상태판 · 운영 마스터 하네스
> **모든 수치는 직접 실측이다. 문서와 실측이 충돌하면 실측을 적고 충돌 사실을 남긴다.**

---

## 1. 우나어는 무엇이고 무엇을 하려는가

**서비스**: 40대 중반~60대 중반 여성(핵심 50대)의 커뮤니티. `age-doesnt-matter.com`
**본질**: 일자리 플랫폼이 아니라 **커뮤니티**다. 일자리는 '돈과 일' 축의 한 기능이다.

### 목적

> 우나어를 네이버와 사용자에게 **다시 신뢰받는 커뮤니티**로 소생시킨다.

코드 정리·인프라 정리·문서 정리·자동화 제거·콘텐츠 정리는 전부 **이 목적의 수단**이다.
목적에 직접 기여하지 않는 작업은 긴급해 보여도 뒤로 보낸다.

### 목표 지표

**North Star = 주간 재방문 참여 유저 수** (최근 7일 재방문 + 글/댓글 1회 이상 고유 사용자)

DAU·MAU·PV·SEO 클릭·색인 수는 **생존/유입 참고지표이지 목표가 아니다.**
현재는 복합 지표를 만들지 않고 **네 숫자를 각각 관찰**한다(§6-E).

### 기대 결과

1. 회원이 글과 댓글을 남기는 커뮤니티로 돌아온다.
2. 네이버가 다시 수집·색인·노출한다.
3. 운영자가 무엇이 돌아가는지 알 수 있는 상태가 된다.
4. 창업자의 판단 피로가 줄어든다 — 지금 할 것과 하면 안 되는 것이 분리된다.

### 하지 않는 일 (불변 금지선)

- 네이버 신뢰가 걸린 콘텐츠를 **근거 없이 대량 삭제**하지 않는다.
- 로그인·SEO·DB·자동화·문서를 **한 번에** 흔들지 않는다.
- 소란소란 코드·데이터를 우나어에 섞지 않는다. **별도 서비스·repo·DB·인프라다.**
- "시니어·어르신·노인·실버" 표현을 쓰지 않는다.
- 네이버 노출면(`sitemap`·`robots`·`canonical`·일반 `<meta robots>`)을 훼손하지 않는다.

---

## 2. 마일스톤

| 시점 | 날짜 | 판정 대상 | 현재 |
|---|---|---|---|
| **T+7** | 2026-09-12 | 생존 1차 — "더 망가지지 않는 상태가 됐는가" | **진행 중** (D-2) |
| **T+14** | 2026-09-19 | 신뢰 정리 — noindex/격리/리라이트/삭제 실행 여부 결정 | 대기 |
| **T+30** | 2026-10-05 | 회복 판정 — 네이버 수집·색인·노출·클릭 + 실회원 반응 비교 | 대기 |
| **T+90** | 2026-12-04 | 사업 판단 — 유지 / 축소 / 병렬 지속 결정 | 대기 |

**T+7 은 성장을 판정하지 않는다.** 목적은 출혈이 멎었는지 보는 것이다.

### T+7 판정 기준 대비 현재

| 기준 | 상태 | 근거 |
|---|---|---|
| 로그인 정상 | **PASS** | `/api/health/auth` 200, 전 항목 true |
| 신규 저품질 누적 중단 | **PASS** | BOT·SHEET 신규 발행 0, `prisma.comment.create` 호출 에이전트 0 |
| 위험 자동화 차단 | **PASS** | `automation_status=PAUSED`, 등록 핸들러 6개, 필수 5개만 실행 |
| 검색 지표 관찰 시작 | **PARTIAL** | 기술 방어선 정상이나 네이버 수집 재개 신호 0 |

---

## 3. 현재 main 반영분

`origin/main = 325ff74e` (2026-09-11 실측) · Rescue 시작(2026-09-05) 이후 누적 merge 진행 중.
**2026-09-10 이후 추가 merge 4건**: #462(폐기 정본) · **#463 R8 측정** · **#464 R8-v2 계측** · **#465 #418 수정**.

| 영역 | 반영 |
|---|---|
| 로그인·인증 | health/auth no-store, 어드민 DB 오류 비노출 (#410) |
| 자동화 차단 | `automation_status=PAUSED` (#409), bot API write 기본 차단 (#412), moderator 생존 (#411) |
| 자동화 제거 | Gate 2 (#434) · C-level 10 (#435) · SUPERSEDED 4 (#436) · GROWTH_LEGACY 13 (#437) · SEED+댓글파동 (#438) · B-5 관측 (#439) · **B-3 외부 카페·시트 공급망 (#440, −28,436줄)** · 잔여 정리 (#442) · KEEP 운영화 (#446) |
| 코드·타입 | ops 타입 오류 91 → **0** (#448), 죽은 코드 5배치 (#428~#432) |
| 캐시·정합성 | sitemap/JOB 캐시 무효화 (#417·#419·#422), cron-links 실측화 (#424·#425), moderator false-green (#426) |
| 보안·의존성 | prod 취약점 46 → **총 11**(high 5 + moderate 6), critical **0** (#451, 2026-09-11 재실측) · **Next 14 → 16.3.4 · React 19** (#452) |
| **이미지 렌더 복구** | **`images.qualities: [75, 80]` (#461 → `0faf2e2f`)** — Next 16 은 허용 목록 밖 `q` 를 400 으로 거부하고 기본값이 `[75]` 다. 저장된 매거진 HTML 의 `q=80` 이 전부 400 이었다(고유 이미지 55건 · 22페이지). 생성 코드만 고쳐서는 복구되지 않아 `80` 을 허용 목록에 남겼다 |
| **콘텐츠 오염 폐기(A-3)** | **네이버 카페 유래 데이터 영구 폐기 도구 (#454 → `e7778998`) 및 production 실행 완료** — Post hard delete 7,125 · tombstone 324 · 봇 댓글 54,496 · `CafePost` 33,031 · `BotLog` 99,578 · R2 객체 504 |
| 문서 | R0 정본화 (#433), 오도 문서 정리 (#414~#416), R5 정본화 (#443), 상태판 갱신 (#441·#445·#453) · **운영 정본 신설 (#456)** · ~~R7 발행 리듬 확정 (#457)~~ **— 잘못된 가정에 근거해 창업자 최종 결정(2026-09-10)으로 대체됨** |
| **콘텐츠 운영(R7)** | **창업자 페르소나 발행 화면 (#455 → `a9835e0e`)** — 창업자가 직접 쓴 글을 기존 페르소나 명의로 발행, `AdminAuditLog` 와 한 트랜잭션 |
| **R8 회원 소생 측정** | **어드민 `/admin/member-recovery` (#463 → `ab2cd44a`)** — 실회원이 끊기는 단계를 한 화면에서 본다. 가입 퍼널·D1 활성화·답글 루프·재방문(기존 `admin.retention.ts` 재사용). production 배포 완료 |
| **R8-v2 배너 CTA 계측** | **(#464 → `a8ff2c9b`)** — `signup_banner_shown`·`clicked` 에 `cta_type`+`measurement_version=r8-v2`. 과거 미버전 계측과 **합산하지 않는다**. `kakao_button_click` rate limit 면제 + 버전 분리. production payload 4환경 실측 완료 |
| **글상세 하이드레이션** | **React #418 수정 (#465 → `325ff74e`)** — 원인은 상대시각이 아니라 `CommentSection` 의 `authKnown` 이 Suspense 경계 밖 세션 상태에 의존한 것. `useSyncExternalStore` server-snapshot 으로 고정. **production 40/40 0건 검증 완료** |
| **검색(병렬 E)** | **목록 SSR 복구 (#458 → `18792f44`)** — 서버 HTML 에 글 링크·페이지네이션을 담는다. `SearchParamsBridge` · page 인지 캐시 쿼리 · 상대시각 hydration 처리 |

---

## 4. 설계 / 구현 / 운영 검증 분리

"구현했다"와 "운영에서 확인했다"는 다르다. 셋을 나눠 적는다.

| 항목 | 설계 | 구현 | **운영 검증** | 검증 근거 |
|---|:--:|:--:|:--:|---|
| 로그인·세션 복구 | ✅ | ✅ | ✅ | production 실제 로그인 + health/auth 200 |
| 자동화 PAUSED | ✅ | ✅ | ✅ | 2026-09-09 23:09 UTC 자연 실행 success |
| Moderation lifecycle | ✅ | ✅ | ✅ | 2026-09-09 16:36 UTC 자연 실행 success |
| GHA DB 인증 | ✅ | ✅ | ✅ | run `34187374847` success · AuthenticationFailed 0 |
| B-3 공급망 제거 | ✅ | ✅ | ✅ | `agents/cafe/` 부재 · 카페 크롤 plist 0 · 신규 발행 0 |
| Next 16 / React 19 | ✅ | ✅ | ✅ | production LCP median 2,264ms · perf 99 · #418 0/20 |
| 네이버 기술 방어선 | ✅ | ✅ | ✅ | **859 URL** 200 · canonical 100% · noindex 0 · Yeti 200 (2026-09-10) |
| 네이버 **색인 회복** | ✅ | — | ❌ | 최근 창업자 증거 기준 색인 0 · **현재값 Search Advisor 재확인 필요**(Claude 접근 권한 없음) |
| 콘텐츠 오염 처분 | ✅ | ✅ | ✅ **완료** | 2026-09-10 `09:32:39Z`=**18:32:39 KST** production 실행 `done ok:true`. 검증표 12항목 전항 PASS — 네이버 유래 Post **0** · tombstone **324** · `CafePost`/`CafeTrend`/`CommentWaveQueue` **0** · R2 잔존 **0** · **글 경로 보존 지표 전부 불변**. 🔴 단, 검증표 밖에서 **댓글 경로 `GuestLike` 82건이 cascade 폐기**됐다(§8) |
| 목록 SSR 크롤 경로(**병렬 E**) | ✅ | ✅ | ✅ **완료** | production 실측 2026-09-10: 6경로 각 **고유 상세 링크 12건** · `page=1`·`page=2` 겹침 0 · #418 140회 0 (PR #458 → `18792f44`). ⚠️ 그 140회는 **홈+목록 6경로** 측정이고 **글상세는 대상이 아니었다** — 글상세 #418 은 2026-09-11 에 별도로 발견·수정됐다(#465) |
| **R7 발행 도구** | ✅ | ✅ **구현 완료** | — | PR #455 → `a9835e0e`. **운영은 창업자 독자 영역이라 Rescue 검증 대상이 아니다** |
| **R8 측정 체계(ⓐ)** | ✅ | ✅ | ✅ **완료** | `/admin/member-recovery` 배포(#463) · r8-v2 계측 배포(#464) · production payload 4환경 실측 |
| **R8 소생 성과(ⓑ)** | ✅ | — | ❌ **관찰 중** | 측정은 끝났고 **성과는 사람이 와야 생긴다**. 배너 노출 135 대비 클릭 0(미버전 구간) |
| 글상세 하이드레이션 #418 | ✅ | ✅ | ✅ **완료** | production `325ff74e` 40/40 0건(글 2종 × desktop/mobile × 10회) |

---

## 5. 완료 / 부분 완료 / 실행 대기 / 미착수

| 구분 | 항목 |
|---|---|
| **완료** | R0 싱크 고정 · R1 로그인 소생 · R4 자동화 단순화 · R5 문서 단순화 · Next 16/React 19 전환 · 이미지 `q=80` 복구(#461) · 보안 업데이트(critical 0) · ops 타입 0 · **병렬 E 목록 SSR 복구**(2026-09-10 production 검증) · **R7 발행 도구**(구현 완료·종결) · **R3 콘텐츠 오염 폐기 실행**(2026-09-10 완료·불가역) · **R8 측정 체계 ⓐ**(#463·#464 배포 완료) · **글상세 #418 수정**(#465 production 검증) · **공개 콘텐츠 인벤토리·1차 분류**(846건 전수, 2026-09-11) |
| **부분 완료** | R2 네이버 신뢰(기술 PASS / 색인 0) · R6 코드·연동(패키지 의존성 감사 미착수) · **R8**(ⓐ 측정 완료 / ⓑ 성과 관찰 중) |
| **실행 대기** | 없음 |
| **미착수** | 브랜드 금지 표현 정정(**실제 SEO 노출면 62건**, §6-C) · Prisma schema ↔ DB FK 불일치 정정 · 공개 콘텐츠 처분 **실행**(인벤토리·1차 분류만 완료) · `source=USER` 22건 정체 확인 · REVIEW 650 세분화 |
| **종결** | **R7 발행 도구** — PR #455 merge 완료. **이후 콘텐츠 운영은 창업자 독자 영역**이라 Rescue 작업 목록에서 뺀다 |

---

## 6. 영역별 진행률

### 6-0. 전체 요약 (2026-09-11)

| 영역 | 진행률 | 직전 | 한 줄 |
|---|---:|---:|---|
| 6-A 기술 기반 | **92%** | 90% | 테스트 1,385 · #418 해소 · 남은 것은 의존성 감사·FK 대조 |
| 6-B 제품 | **78%** | 75% | R8 측정 화면 추가 · 남은 것은 어드민 KPI 표기 정정 |
| 6-C 콘텐츠 | **65%** | 60% | 폐기 완료 + 인벤토리·1차 분류. **77%가 REVIEW — 처분 정책 미완료** |
| 6-D 검색(네이버) | **35%** | 35% | 기술 방어선 정상 · 색인은 **최근 창업자 증거 기준 0**, 현재값 **재확인 필요** |
| 6-E 회원 소생 | **45%** | 15% | **측정 ⓐ 완료**(#463·#464) · 성과 ⓑ 관찰 중 |
| **전체(가중 아님)** | **약 63%** | 약 55% | 우리가 만들 수 있는 것은 대부분 끝났다. **남은 병목은 사람과 네이버다** |

> ⚠️ 백분율은 **마스터 판정치**이며 계측 지표가 아니다(아래 주의 참조). 개별 수치는 전부 실측이다.

### 남은 우선순위 (이 순서로 한다)

1. **REVIEW 650 두 갈래를 병렬로** — ⓐ `source=USER` 비실회원 **19건 정체 확인** · ⓑ 나머지 **631건 세분화**.
   둘은 서로 기다리지 않는다(631건은 전부 `source ≠ USER`)
2. **공개 콘텐츠 처분 실행 결정** — 위 둘 이후. 창업자 결정 필요(§11-3)
3. 🔴 **Quarantine 알림 경로 복구** — `SLACK_WEBHOOK_LOG` 부재(§7)
4. **네이버 Search Advisor 재확인** — 색인 현재값이 미확인이다(§6-D·§11)
5. R8 성과 관찰 → 기반 정합성 → 운영 위생(worktree 38개 정리)

⏳ **Agents — Weekly** 는 2026-09-14 첫 자연 실행 검증 대기다 — **긴급 항목이 아니다.**

> ⚠️ **아래 백분율은 직접 측정값이 아니라 마스터 판정치다.** 계측된 지표가 아니라
> "완료 조건 대비 어디까지 왔나"를 사람이 매긴 값이다. 표 안의 **개별 수치는 실측**이다.
> 산정 기준: 각 영역의 완료 조건을 항목으로 나누고, **운영 검증까지 끝난 항목의 비중**으로 잡았다.
> 배치 종결 시점에만 갱신하며, 지표로 쓰지 않는다.


### 6-A. 기술 기반 — **92%**

| 항목 | 상태 |
|---|---|
| Next 16 / React 19 | ✅ production 검증 완료 |
| prod 취약점 | ✅ critical **0** · **총 11**(high 5 + moderate 6, 전부 major 업그레이드 필요) — 2026-09-11 `npm audit --omit=dev` 실측 |
| ops 타입 오류 | ✅ **0** |
| 테스트 | ✅ **1,385/1,385** 통과 · 88 파일 (`origin/main` = `325ff74e` 실측, 2026-09-11) |
| lint | ✅ error 0 · 경고 84 baseline 고정 |
| 남은 것 | 패키지 의존성 감사, Prisma schema ↔ DB FK 불일치 |

### 6-B. 제품 — **78%**

실측(2026-09-10): 공개 페이지 **42** · 어드민 페이지 **20** · API 라우트 **50**.
2026-09-11: 어드민에 **`/admin/member-recovery`**(R8 회원 소생 측정) 추가.
로그인·글쓰기·댓글·일자리·매거진 정상.
⚠️ 2026-08-19 스코어카드의 "공개 route 33 · API 54" 는 시점이 다르다 — 위가 현재 값이다.
남은 것: 어드민 KPI 표기 정정(`northStar=4`는 실제로 7일 WAU). **목록 SSR 결손은 2026-09-10 해소**(병렬 E).

### 6-C. 콘텐츠 — **65%**

> 폐기 실행(2026-09-10) + **공개 콘텐츠 처분표 확정(2026-09-11)** 으로 판정 근거가 바뀌었다.
> 아래 수치는 **2026-09-11 재실측**이다(Supabase REST read-only).

| 지표 | ① 폐기 전(`c8f996e0`) | ② **2026-09-11 실측** |
|---|---:|---:|
| Post 전체 | 11,718 | **4,595** |
| PUBLISHED | 844 | **846** |
| HIDDEN | 10,602 | **3,542** |
| DELETED | 262 | **197** |
| DRAFT | 10 | **10** |
| SEO_ONLY | *미계측* | **0** |
| 공개 USER | 76 | **76** |
| 공개 BOT | 464 | **464** |
| 공개 SHEET | 299 | **299** |
| 공개 ADMIN | 5 | **7** |

**공개 non-USER 770건**(BOT 464 + SHEET 299 + ADMIN 7) = 공개 면의 **91%**. 폐기 대상이 전부 비공개였으므로 공개 면은 변하지 않았다.

**인벤토리 및 1차 분류 완료 (2026-09-11)** — 846건 전수. 🔴 **최종 처분 정책은 미완료**(650/846 = 77% 가 REVIEW). 상세: [`2026-09-11-public-content-disposition.md`](./2026-09-11-public-content-disposition.md) · CSV 846행

| 판정 | 건수 | 근거 요약 |
|---|---:|---|
| **PRESERVE** | **136** | 실회원 작성 **54** + 실회원 반응 44 + 30일 실조회 38 |
| **REVIEW** | **650** | 두 갈래 — **`source=USER` 비실회원 19** + **나머지 non-USER 631**. 둘은 **병렬 진행 가능** |
| **HIDE** | **60** | 본문 0자 2 + 저품질 58 (전건 `source=SHEET`) |
| **DELETE** | **0** | 🔴 **hard delete 를 권고하지 않는다** — 아래 |

🔴 **hard delete 는 이 표의 권고가 아니다.** 본문 0자 2건도 **HIDE** 로 둔다.
A-3 폐기에서 FK 사각으로 `GuestLike` 82건이 cascade 손실됐고 `Like` 댓글 경로 손실은 지금도 알 수 없다(§8 🔴).
따라서 hard delete 는 **① 대상별 dependency closure 전수 대조 ② 창업자 명시 승인** 둘 다 있을 때만 가능하다.

- **공개 `source=USER` 76건 중 실제 실회원 작성은 54건**이고, 나머지 **22건은 실회원 기준
  (`providerId` 순수 숫자 · `role≠ADMIN`) 미충족**이다(그중 3건은 실반응·실조회가 있어 PRESERVE 로 확정, REVIEW 는 **19건**).
  **19건 정체 확인과 나머지 631건 세분화는 병렬로 진행할 수 있다** — 631건은 전부 `source ≠ USER` 라
  회원 글 경계 판정과 독립적이다.
- **브랜드 금지 표현은 축을 나눠 읽는다** (2026-09-11 · production `generateMetadata` 규칙으로 재측정):

  | 축 | 정의 (production `generateMetadata` 3분기) | 실측 |
  |---|---|---:|
  | **A. 실제 SEO title/description** | 커뮤니티 `seoTitle ?? title` · `seoDescription ?? (summary ‖ fallback)`<br>매거진 `seoDescription ?? (본문 첫 150자 ‖ summary ‖ 고정문구)`<br>**JOB** `seoTitle ?? "${title} — ${company} 채용"` · `seoDescription ?? (본문 첫 100자 · location 근무 · formatSalary)` | **62** |
  | **B. 공개 본문·제목 전체**(CSV `bannedPublicText`) | `title` + 본문 plain text | **80** |
  | **C. slug 문자열** | `slug` (= sitemap URL) | **4** |

  **축 A 62건 boardType 분해: JOB 55 · MAGAZINE 5 · STORY 2** (source: BOT 59 · SHEET 2 · USER 1).
  🔴 **축 A 는 JOB 규칙을 반드시 포함해야 한다** — `boardType=JOB` 191건은 `Job` 모델이 아니라
  **`Post` + `JobDetail` 관계**로 `/jobs/{id}` 를 렌더한다(`getJobDetailPublic(postId)`).
  JOB 을 커뮤니티 규칙으로 계산하면 `company` 가 빠져 축 A 가 틀어진다.
  **`title`+`slug` 를 "URL 노출면"이라 부르면 안 된다** — production 이 내보내는 값은 `seoTitle`/`seoDescription`
  체인이다. 이전 판(`a6361ec9`)의 "URL 노출면 15"는 오류이며 이 판에서 교정했다.
  축 A 62건 중 **55건(89%)이 채용 공고의 공식 직함**(`노인요양`·`여자어르신 재가방문`)이라
  브랜드 카피 위반과 성격이 다르다 — 원문 보존 vs 표기 정정은 정책 결정이다.

- ⚠️ **"검색 유입"은 직접 측정하지 못했다.** `Post.viewCount` 는 봇 시대 누적값이라 쓸 수 없다
  (846건 중 690건이 100 이상인데 사이트 전체 30일 비봇 `page_view` 는 3,201건뿐이다).
  대리지표로 **30일 비봇 `post_view`** 를 썼다 — 실조회가 있는 공개 글 **69건**, 나머지 **777건 0**.

### 6-D. 검색(네이버) — **35%**

| 축 | 상태 |
|---|---|
| 기술 수집 가능 | ✅ **859 URL** 전수 200 · canonical 자기참조 100% · 일반 robots noindex 0 · X-Robots-Tag 0 · Yeti 200 · 404 정상 (2026-09-10) · **폐기 후에도 `<loc>` 859 불변**(대상이 전부 비공개였다) |
| 내부 링크 경로 | ✅ **복구(2026-09-10)** — 6경로 각 고유 상세 링크 **12건**, `page=1`·`page=2` 겹침 0. sitemap 외 발견 경로가 생겼다 |
| 네이버 수집·색인·노출 | ❌ **최근 창업자 증거 기준 색인 0** — 🔴 **현재 Search Advisor 재확인 필요** |

**"기술적으로 수집 가능"과 "실제 색인"은 별개다.**

⚠️ **수집·색인·노출 0 은 이 문서의 최신 직접 실측이 아니다.** Claude 는 네이버 Search Advisor 접근 권한이 없어
**검증하지 못했다.** 마지막 근거는 창업자가 제시한 증거이며, **현재 값은 Search Advisor 재확인이 필요하다**(§11).
재확인 전까지 "지금도 0"이라고 단정하지 않는다.

### 6-E. 회원 소생 — **45%** (측정 ⓐ 완료 / 성과 ⓑ 관찰 중)

**측정 체계(ⓐ)는 배포 완료다.** `/admin/member-recovery`(#463) + r8-v2 배너 CTA 계측(#464).
진행률이 15% → 45% 로 오른 것은 **측정이 생겼기 때문이지 성과가 생겼기 때문이 아니다.**

| 관찰 대상 | 7일 | 30일 | 직전(2026-09-10) |
|---|---:|---:|---|
| 신규 가입(실회원) | **0** | **3** | 7일 0 · 30일 4 |
| 실회원 공개 글 | **14** | **19** | 7일 14 · 30일 19 |
| 실회원 댓글 | **1** | **19** | 7일 0 · 30일 6 |
| 실회원 계정 총계 | \- | **188** | 188 |

실회원 판정 SSoT = `providerId` 가 순수 숫자(카카오 ID) **AND** `role ≠ ADMIN`.
**마지막 실회원 가입은 2026-08-21이다** — 21일째 신규 가입이 0이다.
실회원 댓글 7일 1건은 직전 0에서 **처음 0을 벗어났다**(단일 건이라 추세로 읽지 않는다).

**배너 전환 계측 (r8-v2 배포 이후 관찰 시작)**

- 배포 이전 30일 실측: 배너 노출 방문자 **135** · `signup_banner_clicked` **0** · 사이트 전체 `kakao_button_click` 15 · 실제 신규 실회원 3(ID 대조 3/3 일치).
- 🔴 그 구간에는 **노출 이벤트에 CTA 종류가 없어** "어떤 CTA를 보고 안 눌렀는지" 판정이 **불가능**했다. #464 가 그것을 고쳤다.
- 🔴 **과거 미버전 계측과 r8-v2 를 하나의 전환율로 합산하지 않는다.** 어드민이 두 계열을 분리해 표시한다.
- 경계 기준은 **달력 시각이 아니라 이벤트의 `measurement_version`** 이다 — 배포 뒤에도 캐시된 구버전 클라이언트가 미버전 이벤트를 보낸다.

---

## 7. 실제 활성 자동화

### GitHub Actions — 파일 10개 / **active 6 · disabled 4**

| workflow | 상태 | 최근 **자연**(schedule) 실행 |
|---|---|---|
| Agents — Daily | **active** | 2026-09-10 23:05 UTC **success** |
| Agents — Moderation | **active** | 2026-09-10 16:22 UTC **success** |
| Agents — Weekly | **active** (2026-09-09 재활성화) | ⏳ **첫 자연 실행 2026-09-14 예정** — 아래 |
| CI (Smart QA) | **active** | PR마다 실행 · 전부 success |
| Lighthouse CI | **active** | PR마다 실행 · 전부 success |
| Quarantine Check | **active** | 2026-09-07 04:24 UTC **success** |
| Agents — Job Scraper | `disabled_manually` | — |
| Prewarm Detail Pages | `disabled_manually` | — |
| Push — Scheduled Dispatch | `disabled_manually` | — |
| Run Script (일회성) | `disabled_manually` | — |

⏳ **Agents — Weekly 는 "첫 자연 실행 검증 대기" 다 — 장애가 아니다.**
이 workflow 는 **2026-09-09 재활성화**됐고, 기존 운영 기록상 **다음 자연 실행은 2026-09-14 10:00/10:30 KST**
(cron `0 1 * * 1` · `30 1 * * 1`)다. 마지막 schedule 실행 기록이 2026-08-24 인 것은 재활성화 **이전** 구간이다.
**9/14 실행 전까지는 미실행을 장애로 판정하지 않는다.** 9/14 이후 결과로 판정한다.
담당 태스크: F-12 탈퇴회원 PII 익명화 · PR-E 주간 GSC 스냅샷.

🔴 **Quarantine Check 의 알림 경로가 끊겨 있다.**
`quarantine-check.yml` 은 기한 초과 시 `secrets.SLACK_WEBHOOK_LOG` 로 Slack 알림을 보내는데,
저장소 secrets **21개 중 `SLACK_WEBHOOK_LOG` 가 없다**(존재하는 것은 `SLACK_CHANNEL_LOG`·`SLACK_BOT_TOKEN` 계열).
`curl ... || echo` 로 감싸져 있어 **실패해도 workflow 는 success 로 끝난다** — 초과 항목이 생겨도 아무도 모른다.
현재까지는 초과 항목이 0이라 드러나지 않았다.

### 에이전트 런타임

`automation_status = **PAUSED**` (constitution.yaml). 코드가 파싱하는 값은 이것 하나뿐이다.
PAUSED 에서도 도는 **필수 태스크 5개**:

| 키 | 이유 |
|---|---|
| `coo:moderator` | 금지어 감지·자동 숨김 — 멈추면 유해 콘텐츠가 노출된다 |
| `cto:security-audit` | 로그인 실패·어드민 민감 액션 감사 |
| `cto:count-reconcile` | 비정규화 카운트 정합성(멱등) |
| `cto:anonymize-withdrawn-apply` | 30일 경과 탈퇴자 PII 익명화 |
| `cmo:seo-snapshot` | Google Search Console read-only 관측 |

등록 핸들러는 **6개**다 — 위 필수 5개 + `coo:job-scraper`(workflow 가 `disabled_manually` 라 실제로는 돌지 않는다).
B-3 제거로 카페·시트 계열은 전부 사라졌다.
⚠️ PR #442 는 "핸들러 17 → 7" 로 보고했으나 **현재 main 실측은 6개**다.

### launchd (로컬 머신) — **우나어 관련 2개 loaded** (2026-09-11 재실측, 변화 없음)

| job | 상태 |
|---|---|
| `com.unao.unao-prod-sync` | loaded · exit 0 |
| `com.unaeo.opsboard` | loaded · PID 524 |

**우나어 카페 크롤러 plist(`com.unao.cafe-crawler-*`)는 0개다.** 재수집 경로가 없다.

---

## 8. DB · env · workflow · launchd 실제 상태

### DB (Supabase, read-only 집계)

> **세 시점을 섞지 않는다.** ①은 `c8f996e0` 폐기 전 측정, ②는 실행 직전, ③은 폐기 후 실측이다.
> ①을 "실행 직전"으로 인용하지 않는다. 상세·근거는 `2026-09-10-naver-cafe-purge-facts.md` §9-E.

| 테이블 | ① 폐기 전 측정 | ② 실행 직전 | ③ 폐기 후(2026-09-10) | ④ **2026-09-11 재실측** |
|---|---:|---:|---:|---:|
| Post | 11,718 | 11,720 | 4,595 | **4,595** |
| BotLog | 125,025 | 125,026 | 25,448 | **25,451** |
| Comment | 67,086 | 67,086 | 12,590 | **12,590** |
| Like | 31,187 | *미계측* | 16,612 | **16,612** |
| GuestLike | 283 | 283 | 201 | **201** |
| CafePost / CafeTrend / CommentWaveQueue | 33,031 / 191 / 276 | 33,031 / 191 / 276 | 0 / 0 / 0 | **0 / 0 / 0** |
| User | 508 (실회원 188) | *미계측* | 508 | **508** (실회원 **188** · `role=ADMIN` **0**) |
| Notification | 1,010 | *미계측* | 1,010 | **1,010** |
| Report | *미계측* | *미계측* | *미계측* | **1** |
| DailyKpiSnapshot | 61 (2026-06-29~08-23 **정지**) | *미계측* | 변화 없음 | **61** (여전히 정지) |

④는 ③ 이후 하루 경과분이다. `BotLog` +3 외에 변동이 없다 — **재오염 신호가 없다.**

`Comment` 감소 **54,496** 은 실행 `start-state` 가 직접 측정한 봇 댓글 수와 **정확히 일치**한다.
②의 `Post` 11,720 · `BotLog` 125,026 은 ③ + 폐기량 역산이다 — 실행 22분간 신규 Post 는 0건이었다.

**소비처 0 모델**(잔여, 2026-09-11 재실측): `ChannelDraft` **653** · `SocialPost` **119** · `NaverBlogQueue` **16** · `UserPostWaveQueue` **11**.

⚠️ `NaverBlogQueue` 는 PK 가 `id` 가 아니라 **`queueId`** 다. `select=id` 로 조회하면 REST 가 **HTTP 400** 을 돌려준다 —
**테이블 부재나 노출 제외가 아니다.** `select=queueId`(또는 `*`)로 재측정하면 **16건**이다.
`CommentWaveQueue` 276 은 A-3 폐기로 **0** 이 됐다.

**Prisma schema ↔ 실제 DB 불일치**: `HomeCurationOverride.postId` 가 schema 는 Cascade, DB 는 **RESTRICT**다(migration 기준). 모르고 삭제 코드를 짜면 런타임에 막힌다.

🔴 **A-3 에서 드러난 FK 사각(불가역 손실 발생)**: 처분 설계가 `Post` 의 자식 FK 만 봤다.
`GuestLike.commentId`·`Like.commentId` 는 **`Comment`** 를 CASCADE 로 참조한다. 봇 댓글 54,496건을 지우면서
**그 댓글에 달려 있던 게스트 공감 82건이 함께 사라졌다**(GuestLike 283 → 201, 글 경로 160 은 불변).
`Like` 의 댓글 경로 손실 건수는 사전 분해 계측이 없어 **알 수 없다**. 상세: `2026-09-10-naver-cafe-purge-facts.md` §9-C.
→ 후속 2(기반 정합성)의 FK 전수 대조는 **`Post` 뿐 아니라 함께 지우는 모든 테이블의 자식**을 훑는다.

### env

| 위치 | 키 수 | 비고 |
|---|---:|---|
| `unao-prod/.env.local` | **105** | 운영 실행용. DATABASE_URL·DIRECT_URL·Supabase·Auth·Kakao·R2·Anthropic·Google 서비스계정 **존재** |
| `unao-main/.env.local` | **4** | 개발 워크스페이스. DB 접속정보 **없음** |

**Slack 경로는 살아 있다.** 실제 연결은 `SLACK_BOT_TOKEN` + `SLACK_CHANNEL_*` 이며
로컬 env 에 Slack 키 **22개**, GitHub Secrets 에 **8개**가 존재한다(`SLACK_WEBHOOK_URL` 은 이 경로에 쓰이지 않는다).

⚠️ **DB 접속 범위 구분** — 셋을 섞지 않는다.

| 경로 | 상태 |
|---|---|
| production runtime (Vercel → Supabase) | ✅ **정상** — `/api/health` `database: ok` |
| GitHub Actions DB 연결 | ✅ **정상** — run `34187374847` success · AuthenticationFailed 0 |
| **로컬 Prisma/`pg` 경로**(`DATABASE_URL`·`DIRECT_URL`) | ❌ **여전히 실패**(2026-09-11 재확인) — `DIRECT_URL` 호스트 DNS 미해석 · `DATABASE_URL` password authentication failed. 로컬 스크립트만 영향 |
| **Supabase REST**(service role, GET) | ✅ **정상** — 이번 배치의 모든 DB 실측은 이 경로다 |

**env 값은 이 문서 어디에도 적지 않는다.**

### 배포

production **`2026.09.11-325ff74`** · main **`325ff74e`** (2026-09-11 실측)

| 경로 | 결과 |
|---|---|
| `/` · `/community/stories` · `/magazine` · `/jobs` | 200 |
| `/api/health` | 200 · `status: healthy` · `database: ok` |
| `/api/health/auth` | 200 · `ok·db·authSecret·kakao` 전부 true |
| `/sitemap.xml` | 200 · **859 URL** (폐기 전후·이번 배포 후 불변) |
| `/robots.txt` | 200 · `Allow: /` + `Disallow: /admin/ /api/ /my/` |
| `/admin/*` (익명) | 307 → `/admin/login` |

**글상세 React #418**: production 40/40 **0건**(글 2종 × desktop 1440/mobile 390 × 10회, #465 배포 후).

### 열린 PR · worktree

**열린 PR 0건** (2026-09-11 실측) — 단, **이 정본을 갱신하는 PR #466 자체는 제외한 값**이다.
PR #466 을 포함하면 열린 PR 은 1건이다.

| PR | 상태 |
|---|---|
| ~~#462~~ 폐기 결과 정본 | merge 완료 `3f8b3aff` |
| ~~#463~~ R8 회원 소생 측정 | **merge 완료** `ab2cd44a` · production 배포 |
| ~~#464~~ R8-v2 배너 CTA 계측 | **merge 완료** `a8ff2c9b` · production payload 4환경 실측 |
| ~~#465~~ 글상세 React #418 수정 | **merge 완료** `325ff74e` · production 40/40 검증 |

로컬 worktree **전체 44개** — **dirty 5 · clean 39**(2026-09-11 실측).

| worktree | 미커밋 파일 |
|---|---:|
| `New_Claude_agenotmatter` | 47 |
| `unao-main` | 44 |
| `admin-unified-dashboard` | 4 |
| `admob-min` | 3 |
| `mag-revalidate-wt` | 2 |

🔴 **dirty 5개는 건드리지 않는다** — 미커밋 작업이 유실된다.
**clean 39개** 중 **현재 PR 작업본(`unao-ops-sync`) 1개를 제외한 38개**가 즉시 정리 후보다.
즉 `39 = 38(즉시 후보) + 1(진행 중 PR)` 이다 — 두 숫자를 섞지 않는다(§11-6).

---

## 9. 남은 작업 전체와 의존관계

```
[A] 네이버 회복 축   ※ 목록 SSR 복구는 여기 넣지 않는다 — 아래 [E] 하나로만 관리한다
 A-2 브랜드 금지 표현 정정 (**실제 SEO 노출면 62건** · 본문 80 · slug 4) ──┐
 A-3 콘텐츠 처분 실행(#454) **완료** ┼─→ A-4 수집 재개 관측 → (외부) 네이버 색인
 [E] 목록 SSR 복구 ─────────────┘        ↑ 창업자·네이버 의존

[B] 콘텐츠 신뢰 축
 B-1 인벤토리·1차 분류 **완료(846건)**
      ├─ B-1a REVIEW 갈래 A: source=USER 비실회원 19건 정체 확인 ──┐  ※ a·b 는 병렬
      └─ B-1b REVIEW 갈래 B: 나머지 non-USER 631건 세분화 ─────────┼─→ B-2 처분 실행(창업자 결정)
 B-3 저품질 판정 **완료** — 300자 미만 355 / HIDE 권고 60 ─────────┘

[C] 기반 정리 축  (A·B 와 독립)
 C-1 Prisma schema ↔ DB FK 불일치 정정
 C-2 소비처 0 DB 모델 5종 처분
 C-3 패키지 의존성 감사
 C-4 어드민 KPI 표기 정정 · DailyKpiSnapshot 정지 처리

[D] 운영 위생 축  (독립)
 D-1 문서 정본 정리(이 문서로 통합)
 D-2 회복 관측 체계 상시화
 D-3 worktree 정리 (clean 39 중 진행 PR 1 제외 → 38 후보)

[R7] 발행 도구   ← **종결**. 구현 완료(PR #455). 이후 운영은 창업자 독자 영역이라
                    이 도식에 남은 작업이 없다

[R8] 회원 소생 축   ⓐ 측정 **완료(#463·#464 배포)** / ⓑ 성과 **관찰 중**
 R8-1 가입 전환 ✅ ─→ R8-2 첫 글·첫 댓글 ✅ ─→ R8-3 답글 순환 ✅ ─→ R8-4 D1/D7 재방문 ✅
 남은 것: **사람이 오는 것**(ⓑ). 측정으로는 충족되지 않는다

[E] 네이버 SSR 복구   ← **독립 병렬 기술 배치**. R7·R8 과 서로 기다리지 않는다
 목록 SSR 크롤 경로 복구  ← **이 작업의 유일한 표기다**(A-1·E-1·배치 1 이라 부르지 않는다)
```

**R8 측정 체계는 R7 과 독립으로 만든다.** 다만 운영 성과(ⓑ)는 사람이 와야 생기는 값이라
측정만으로 충족되지 않는다.

**의존의 핵심**: A-4(수집 재개)는 우리가 만들 수 없다. A-2·A-3·E 를 끝내도 **네이버가 다시 올 때까지 기다려야 한다.** 그래서 A 축은 "할 수 있는 것을 끝내고 관측한다"가 전부다.

---

## 10. 앞으로의 실행 배치

### 실행 우선순위

| 순위 | 배치 | 성격 | 상태 |
|---|---|---|---|
| **1a** | **`source=USER` 비실회원 19건 정체 확인** | REVIEW 갈래 A | ⏳ 선행 (**1b 와 병렬**) |
| **1b** | **나머지 631건 세분화** | REVIEW 갈래 B — `source ≠ USER` 라 1a 와 독립 | ⏳ 선행 (**1a 와 병렬**) |
| **2** | 공개 콘텐츠 처분 **실행**(B-2) | 1a·1b 가 끝나야 의미가 생긴다 | 대기 |
| **4** | 🔴 **Quarantine 알림 경로 복구** | `SLACK_WEBHOOK_LOG` 부재로 초과 알림이 사라진다 | 신규 발견 |
| **5** | R8 성과 관찰(ⓑ) | 측정은 끝났다. v2 누적 후 CTA별 전환 해석 | 관찰 |
| **6** | 기반 정합성(C-1·C-2·C-4) | FK 대조 · 소비처 0 모델 · KPI 표기 | 미착수 |
| **7** | 운영 위생(D-2·D-3) | 관측 상시화 · worktree 38개 정리 | 미착수 |
| ⏳ 검증 대기 | **Agents — Weekly 첫 자연 실행**(2026-09-14) | 장애 아님. 9/14 결과로 판정 | 대기 |
| ~~완료~~ | R7 발행 도구 · 병렬 E SSR · A-3 폐기 · **R8 측정 ⓐ** · **#418 수정** | — | ✅ |

아래 배치 설명은 이 순서대로 읽는다.

### R7 — 창업자 페르소나 발행 도구 (**구현 완료 · 종결**)

| | |
|---|---|
| 범위 | 창업자가 직접 쓴 글을 어드민에서 발행하는 **도구**. PR #455 → `a9835e0e` |
| 상태 | **구현 완료 · Rescue 범위에서 종결** |
| 🔴 관리 범위 밖 | **글 작성 · 발행 빈도 · 주제 · 운영 여부는 전부 창업자 독자 결정이다.** Rescue 도, Claude·Codex 도 관여하지 않는다 |
| 남은 작업 | **없음.** 운영 검증·발행 리듬·주제 축 확정은 이 문서의 작업 목록이 아니다 |
| 🚫 금지 | **가짜 댓글·좋아요·회원 행세 자동화를 다시 만들지 않는다.** 이것은 도구 쪽 금지선이라 계속 유효하다 |

**발행 주체는 세 층으로 나뉜다. 섞어 읽으면 안 된다.**

| 층 | 누구 | 어디에 남나 |
|---|---|---|
| **콘텐츠 원문 작성 · 발행 조작** | **창업자** | 사람이 직접 쓰고 어드민에서 직접 발행한다. 자동 생성이 아니다 |
| **공개 작성자 명의** | **PR #455 allowlist 의 기존 페르소나** | 글의 `authorId` 만 페르소나 계정이다. 화면에 보이는 이름은 페르소나다 |
| **운영 책임 추적** | **창업자 어드민 계정** | `AdminAuditLog` 에 남는다. `Post` 생성 · `postCount` 증가와 **한 트랜잭션**이라 감사 로그가 실패하면 글도 남지 않는다 |

- **공개 화면에 창업자 개인정보를 노출한다는 뜻이 아니다.** 독자가 보는 것은 페르소나 명의뿐이다.
- 그럼에도 **자동 생성·가짜 회원 활동이 아니다.** 원문은 창업자가 직접 쓴 글이고,
  `source=ADMIN` 과 감사 로그가 그 사실을 데이터에 남긴다. 봇이 스스로 만든 글과 구분된다.

⚠️ **PR #457 정정** — 그 문서 변경은 "발행 빈도를 Rescue 가 관리한다"는 **잘못된 가정** 위에 있었다.
창업자 최종 결정(2026-09-10)으로 **대체됐다.** 발행 빈도·주제·운영 여부는 합의된 적이 없고,
이 문서가 정할 대상도 아니다.

### 주력 — R8 회원 소생

| | |
|---|---|
| 범위 | 가입 전환 · 첫 글/첫 댓글 전환 · 답글 순환 · D1/D7 재방문을 **측정**한다 |
| 왜 | 지금은 "7일 신규가입 0 · 실회원 댓글 0" 이라는 결과만 있고 **어디서 끊기는지**를 모른다 |
| 선행 | **없음.** 측정 체계는 콘텐츠 공급과 무관하게 만들 수 있다. 다만 ⓑ 운영 성과는 사람이 와야 생긴다 |
| **완료 조건 ⓐ 구현 완료** | 가입 전환 · 첫 글/첫 댓글 · 답글 순환 · **D1/D7 재방문** 측정 체계가 **실제로 동작**한다. 숫자가 0이어도 이 조건은 충족될 수 있다 |
| **완료 조건 ⓑ 운영 성과** | 실회원 **신규가입 · 댓글 · 답글 순환**이 **0을 벗어난다**. 측정만으로는 충족되지 않는다 |
| 구분 이유 | 둘을 섞으면 "측정 체계를 만들었으니 완료"라고 말하게 된다. **ⓐ는 우리가 만들 수 있고 ⓑ는 사람이 와야 생긴다** |
| 🚫 금지 | **가짜 댓글·좋아요·회원 행세 자동화 금지.** 지표를 만들기 위해 봇을 다시 켜지 않는다. 숫자가 0이면 0으로 기록한다 |
| 위험 | 낮음(측정). 단, "지표를 올리려 봇을 켜는" 유혹이 가장 큰 구간이다 |


---

### 병렬 E — 네이버 목록 SSR 복구

| | |
|---|---|
| 범위 | 커뮤니티·매거진·일자리 목록의 글 링크를 서버 HTML 에 넣는다 |
| 왜 | 현재 목록의 서버 HTML 은 380자(내비·푸터)뿐이고 **글 링크가 0건**이다. JS 를 실행하지 않는 수집기는 sitemap 외에 글을 발견할 길이 없다 |
| 조건 교정 (2026-09-10) | 앞서 둔 "SSR 텍스트 ≥ 2,000자"는 **6경로 중 5개가 미달**이었다. 목록 글자 수는 글 제목·미리보기 길이에 좌우되는 값이라 **억지로 늘릴 성질이 아니다**. 판정을 글자 수 하나에서 **링크 수·페이지별 차이·빈 shell 대비 배수** 셋으로 바꿨다 |
| 완료 조건 | **대상 6경로 각각** ① 고유 상세 링크 **12개 이상** ② `page=1` 과 `page=2` 의 링크 집합이 **서로 다름** ③ `<script>`·`<style>` 제거 후 visible text **1,000자 이상이면서 기존 빈 shell 대비 3배 이상** / 공통 ④ 기존 UX·필터·정렬·검색·페이지네이션·뒤로가기 불변 ⑤ #418 desktop·mobile 각 10회 0 ⑥ CI·Lighthouse 회귀 없음 |
| 대상 6경로 | `/community/stories` · `/community/humor` · `/community/life2` · `/community/menopause` · `/magazine` · `/jobs` |
| 기존 빈 shell 기준 | 371~416자(실측 2026-09-10 production). 3배 = **1,113~1,248자** |
| ✅ **완료 (2026-09-10)** | PR #458 → `18792f44` · production `2026.09.10-18792f4` 에서 실측 통과 |
| 위험 | 낮음. 데이터 무변경, 되돌리기 쉬움 |

**production 실측 (2026-09-10, Yeti UA 원시 HTML)**

| 경로 | page=1 링크 | page=2 링크 | 두 집합 겹침 | visible text |
|---|---:|---:|---:|---:|
| `/community/stories` | **12** | 12 | **0** | 2,293자 |
| `/community/humor` | **12** | 12 | **0** | 1,684자 |
| `/community/life2` | **12** | 11 | **0** | 1,658자 |
| `/community/menopause` | **12** | 2 | **0** | 1,986자 |
| `/magazine` | **12** | 12 | **0** | 1,257자 |
| `/jobs` | **12** | 12 | **0** | 1,353자 |

page=2 가 12건 미만인 곳은 **글 자체가 그만큼뿐**이다(life2 23건 · menopause 14건).
직전 빈 shell 은 371~416자였다 — 전 경로가 **3배 이상**이다.

함께 확인: 홈+6경로 **#418 desktop·mobile 각 10회 = 140회 전부 0** · pageerror 0 ·
필터·정렬·검색(9건)·페이지네이션·뒤로가기 정상 · `/api/health` `/api/health/auth` `/sitemap.xml` 200 · sitemap **859 URL**.
잔여 console 오류는 광고·폰트·동의관리(doubleclick·fundingchoices·fonts.googleapis) 등 **제3자 스크립트**이며 이 변경과 무관하다.

### 종결 — 네이버 카페 데이터 폐기 (A-3) **실행 완료**

| | |
|---|---|
| 범위 | PR #454 의 도구로 실제 폐기 |
| 완료 조건 | runbook 검증표 12항목 전부 PASS |
| 위험 | **높음 · 불가역** |
| 결과 | **2026-09-10 실행 완료** `09:10:12~09:32:39Z` = **18:10:12~18:32:39 KST** · `done ok:true` · 검증표 전항 PASS · ABORT 0 |
| 폐기량 | Post hard delete **7,125** · tombstone **324** · 봇 댓글 **54,496** · `BotLog` **99,578** · `CafePost` **33,031** · `CafeTrend` **191** · `CommentWaveQueue` **276** · R2 객체 **504** · 로컬 잔재 **76파일** |
| 보존 | **실회원이 작성한 글과 댓글 71건 보존.** 글 경로로 측정한 보존 지표는 전부 불변 — NULL 댓글 56 · 글 GuestLike 111 · Report 1 · HomeCurationOverride 139 · 공개 USER Post 76. **"실회원 콘텐츠·댓글·공감을 보존했다"는 포괄적 단정은 쓰지 않는다**(아래 손실 참조) |
| 🔴 부수 손실 | 봇 댓글 삭제로 **`GuestLike` 82건이 cascade 삭제**(댓글 경로, 사전 미계측·불가역). **`Like` 댓글 경로의 정확한 손실과 실회원 공감 포함 여부는 사전 분해 계측이 없어 알 수 없다.** facts §9-C |
| 운영 결정 | **전체 백업 복원은 하지 않는다**(2026-09-10, 창업자). 손실 행은 의도적으로 폐기한 봇 댓글의 자식이고, 전체 복원은 **폐기 데이터 부활 + 이후 정상 데이터 손상** 위험이 더 크다. facts §9-H · runbook §5-0 |
| 남은 것 | 백업 보존 기간 확인 전까지 **폐기 도구를 지우지 않는다** — 다른 사유로 복원하게 되면 재실행이 필수 게이트다. runbook §5-1·§8 |

### 후속 1 — 콘텐츠 처분표 확정 (B-1·B-3)

| | |
|---|---|
| 범위 | 공개 non-USER **770건**(BOT 464 · SHEET 299 · ADMIN 7, 2026-09-10 폐기 후 실측)을 보존·noindex·리라이트·삭제 후보로 분류한 **판정표**. 실행 아님 |
| 완료 조건 | 770건 전건 분류 · 각 분류의 근거 지표(반응·검색·중복·분량) 명시 · 사용자 영향 건수 산출 · 실행 0 |
| 위험 | 없음 (read-only) |

### 후속 2 — 기반 정합성 정리 (C-1·C-2·C-4)

| | |
|---|---|
| 범위 | Prisma schema ↔ DB FK 전수 대조 및 정정안 · 소비처 0 모델 **4종**(`CommentWaveQueue` 는 A-3 로 소멸) 처분안 · 어드민 KPI 표기 정정 |
| 완료 조건 | 46개 모델 FK 대조표 · 불일치 목록과 정정안(migration 없음) · KPI 표기 정정 PR · **A-3 FK 사각 재발 방지**: 자식 FK 를 `Post` 뿐 아니라 함께 지우는 모든 테이블에 대해 훑는다(§8 🔴) |
| 위험 | 낮음. schema 는 설명이고 DB 를 바꾸지 않는다 |

### 후속 3 — 회복 관측 상시화 (D-2)

| | |
|---|---|
| 범위 | 수집 재개·색인·노출과 실회원 4지표를 주 2회 read-only 기록 |
| 완료 조건 | 관측 절차·기록 양식 확정 · T+30 판정에 쓸 시계열 확보 |
| 위험 | 없음 |

## 11. 창업자 결정이 필요한 항목

**여기 있는 것만 창업자 결정이 필요하다.** 나머지는 실측·판정으로 처리한다.

| # | 항목 | 왜 창업자여야 하나 | 지금 막고 있는 것 |
|---|---|---|---|
| ~~1~~ | ~~PR #454 폐기 **실행 지시**~~ | **2026-09-10 완료** — 창업자 최종 승인 후 실행·검증 종결 | 없음 |
| **2** | Supabase **백업 만료 시점 콘솔 재확인** — 보존 기간 **7일**(창업자 확인). 삭제 전 백업의 실제 만료는 **약 2026-09-17**로 추정된다. 🔴 Claude 는 콘솔 접근 권한이 없어 **검증하지 못했다** | 콘솔 접근 권한 | ① **"백업에서도 소멸"을 말할 수 없음** — 폐기는 라이브 DB 기준이고 daily backup 은 남아 있다 ② 보존 기간이 확정되기 전까지 **폐기 도구를 제거할 수 없다**(복원 시 필수 재실행 게이트). ※ **전체 복원 자체는 하지 않기로 결정**됐다(2026-09-10, facts §9-H) — 이 항목은 "언제 소멸하나"와 "도구 제거 시점"만 막고 있다 |
| **3** | **공개 콘텐츠 처분 실행** — 인벤토리·1차 분류 완료(2026-09-11). PRESERVE 136 · REVIEW **650**(USER 19 + 나머지 631) · HIDE 60 · **DELETE 0**. 선행 3-c·631 세분화 이후 | 콘텐츠 정책 판단 | B-2 실행 전체 |
| **3-b** | **SEO 노출면 금지 표현 62건** 정정 범위 — 상당수가 `boardType=JOB` 채용 공고의 공식 직함이다 | 원문 보존 vs 브랜드 표현 | A-2 |
| **3-c** | **`source=USER` 비실회원 19건 정체 확인 주체·기준**(REVIEW 갈래 A — 갈래 B 631건과 **병렬 가능**) | 데이터 이력 판단 | B-2 실행 |
| **3-d** | **hard delete 검토 여부** — 검토 시 **대상별 dependency closure 전수 대조가 선행**(A-3 FK 사각 재발 방지) | 불가역 · 데이터 손실 위험 | 삭제 경로 전체 |
| **4** | `DIRECT_URL` 인증 정정 | 운영 자격증명 | Prisma 경로 DB 작업 전부 |
| **5** | PR merge 승인 (신규 PR) | 배포 권한 | 모든 배치의 반영 |
| **7** | **네이버 Search Advisor 재확인** — 수집·색인·노출 현재값. Claude 는 접근 권한이 없어 검증하지 못했다 | 콘솔 접근 권한 | 6-D 판정·T+30 회복 판정 |
| **6** | worktree 정리 승인 — 전체 **44** = dirty **5**(제외) + clean **39**. 그중 진행 중 PR 작업본 1개를 뺀 **38개**가 즉시 후보 | 미커밋 작업 유실 위험 | D-3 |
| ~~7~~ | ~~R7 발행 리듬~~ | **철회 2026-09-10** — 발행 빈도·주제·운영 여부는 **창업자 독자 결정**이라 이 문서의 결정 항목이 아니다 | 없음 |

**창업자 결정이 필요 없는 것** (Claude·Codex 가 처리): 처분표 작성, FK 대조, KPI 표기 정정, 관측 기록, 문서 정리.

---

## 12. 문서 정본 정리

### 이 문서로 대체 — 운영 기준으로 쓰지 않는다

| 문서 | 처리 | 이유 |
|---|---|---|
| `docs/operations/UNAO_RESCUE_STATUS.md` | **대체** | 진행률·현재 상태는 이 문서로 통합. 과거 실측 기록은 보존 |
| `docs/ops/OPERATING_MASTER_HARNESS.md` | **부분 대체** | 문서 지도·역할 분담은 이 문서로. PR 분할·운영검증 기준은 **계속 유효** |

### 계속 정본 — 이 문서와 함께 본다

| 문서 | 역할 |
|---|---|
| `docs/operations/2026-09-05-unao-rescue-mode-master-plan.md` | Rescue 전략·금지선의 **원본 근거**. 이 문서는 그 요약이 아니라 실행 상태다 |
| `CLAUDE.md` · `AGENTS.md` | 세션 규칙·역할 |
| `docs/features/REGISTRY.md` | 기능 라이프사이클 |
| `docs/operations/2026-08-20-database-disaster-recovery.md` | DB 재해복구 |
| `docs/operations/2026-09-11-public-content-disposition.md` + `data/*.csv` | **공개 콘텐츠 인벤토리·1차 분류(846건 전수)**. read-only 이며 실행 계획이 아니다. 최종 처분 정책은 미완료 |
| `docs/operations/2026-09-10-naver-cafe-purge-facts.md` · `-runbook.md` | 폐기 실측·**실행 기록**(facts §9)·복원 게이트 절차. **2026-09-10 실행 완료** — 감사 증빙이자 백업 복원 시 필수 절차다 |

### 재감사 대기 — 구현 근거로 쓰지 않는다

`docs/constitution/NORTH_STAR.md` · `agents/core/constitution.yaml`(automation_status 제외) · `docs/constitution/RULE_MAINTENANCE.md`
— 파일명에 `constitution` 이 있어도 정본으로 자동 인정하지 않는다.

### 역사 기록 — 현재 상태로 읽지 않는다

`docs/operations/2026-08-21-unao-as-is-scorecard.md`(2026-08-19~21 시점) ·
`docs/operations/2026-09-05-claude-foundation-reset-audit-report.md` ·
`docs/features/A01-cafe-crawler.md` · `A04-external-content.md` (둘 다 ARCHIVED)

---

## 13. 이 문서를 갱신하는 규칙

1. **수치는 실측만 적는다.** 문서에서 옮겨 적지 않는다.
2. 문서와 실측이 충돌하면 **실측을 적고, 충돌 사실을 남긴다.**
3. "완료"는 **운영 검증까지** 끝났을 때만 쓴다(§4 3단 분리).
4. 진행률은 배치 종결 시점에만 갱신한다. 매일 고치지 않는다.
5. 창업자 결정 항목(§11)은 **정말 창업자여야만 하는 것**만 둔다.
