# 소란소란 브랜드 / 디자인 정본

> **작성** 2026-08-22 (AS-IS 11축 감사 후속) · **분류** 브랜드 / 디자인 / 시각 정체성
> **적용 대상** 소란소란(soransoran.com) 전용. **우나어 작업에는 적용하지 않는다.**
> **한 줄** 소란소란은 우나어의 색만 바꾼 서비스가 아니다. 뼈대는 승계하고 껍데기는 전량 새로 만든다.

---

## 0. 이 문서의 위치

AS-IS 11축 감사에서 우나어의 브랜드/디자인 자산을 전수 조사한 결과,
**디자인 시스템은 재사용 가치가 높고 브랜드 자산은 전량 폐기 대상**이라는 결론이 나왔다.

`m3-new-brand-readiness.md` §13이 *"11축 D-day 산출물 목록이 지금 비어 있다"* 고 적어둔 공백을
이 문서가 메운다. `soransoran-d7-dday-milestone.md` §2 「브랜드 자산 착수」의 체크리스트는
이 문서를 기준으로 판정한다.

### 관련 문서

| 문서 | 관계 |
|---|---|
| `m3-new-brand-readiness.md` | 상위 — M3 전체 준비 상태 (§3 병렬 SEO 전략, §13 D-day 운영 기준) |
| `soransoran-d7-dday-milestone.md` | 상위 — D-7~D+90 작전표 (§1 blocker, §2 브랜드 자산, §4 IA 정본) |
| `soransoran-agent-guidelines.md` | 병렬 — 작업 지침 (§0 적용 범위) |
| `cron-job-dispatcher-ops.md` | 무관 — 스케줄러 인프라 |

---

## 1. 현재 판정

### 🟢 브랜드 방향 — 확정

```
방향 A 「조용한 신뢰」 확정   (창업자 결정 2026-08-22)
로고 D-day 형태  텍스트 워드마크 출발
OG 방향          인물 사진 없이 타이포 중심
```

이 3건은 **재논의 대상이 아니다.** 이후 작업은 이 전제 위에서만 진행한다.

### 🟡 우나어 디자인 시스템 — 일부 승계

우나어의 디자인 토큰·타이포·터치타겟·아이콘 체계는 성숙도가 높다.
WCAG AA 대비를 주석에 명시하는 규율까지 자리잡혀 있다. **버리는 것이 손해다.**

### 🔴 우나어 브랜드 자산 — 전량 폐기

로고 4종·OG 2종·favicon 5종·아이콘 11종·컬러 `#FF6F61`·브랜드 문자열 369건.
**하나도 가져가지 않는다.** §6 참조.

### 판정 요약

| 축 | 판정 | 근거 |
|---|---|---|
| 브랜드 방향 | 🟢 GREEN | A안 확정. 결정 병목 해소 |
| 디자인 시스템 | 🟢 GREEN | 승계 범위 확정 (§4·§5) |
| 브랜드 자산 | 🔴 RISK | 소란소란용 0건. P0 8종 미제작 (§7) |
| 분리 난이도 | 🟡 PARTIAL | 하드코딩 부채가 있어 "복사"가 아니라 "재배선"이 필요 (§13) |

---

## 2. 브랜드 원칙

> 이 7개는 모든 디자인 판단의 상위 기준이다. 개별 결정이 원칙과 충돌하면 원칙이 이긴다.

```
1. 소란소란은 우나어의 색만 바꾼 서비스가 아니다.
   구조를 빌리되 인상은 독립해야 한다.

2. 병원·의료·건강기능식품 사이트처럼 보이면 안 된다.
   갱년기를 "질병"으로 규정하는 순간 커뮤니티가 아니게 된다.

3. 과한 분홍 · 보라 · 병원 민트/시안 · 우나어 코랄을 쓰지 않는다.

4. "여성 건강 = 핑크" 클리셰를 쓰지 않는다.
   가장 흔하고 가장 성의 없는 선택이다.

5. 활발한 척하지 않는다.
   접속자 수·실시간 배지·과장된 알림은 실회원이 적을 때 즉시 들통난다.
   → 정본: "실제 유저 참여는 열고, 봇이 활발한 척하는 것은 막는다"

6. 첫 화면 3초 안에
   "40대 50대 여성이 자기 이야기를 할 수 있는 갱년기 중심 커뮤니티"임이 보여야 한다.

7. 광고처럼 보이지 않는다.
   타겟 사용자는 광고성 화면에 대한 경계심이 높다. 과한 CTA·팝업·형광색을 피한다.
```

---

## 3. 확정된 브랜드 방향 — 「조용한 신뢰」

| 항목 | 정본 |
|---|---|
| **방향명** | 조용한 신뢰 |
| **핵심 감정** | 털어놔도 안전하다 · 소란스럽지 않다 · 광고 같지 않다 |
| **컬러 계열** | 따뜻한 뉴트럴 기반 + 차분한 테라코타/브라운 계열 포인트 |
| **베이스 톤** | 웜 아이보리 / 웜 그레이 — 순백보다 눈이 편하고 신뢰감이 높다 |
| **피해야 할 색** | `#FF6F61`(우나어 코랄) · 핑크 · 보라 · 병원 민트/시안 · 형광 계열 |
| **로고 방향** | 텍스트 워드마크 우선. 심볼 없이 출발 (§9) |
| **OG 방향** | 인물 사진 없이 타이포 중심 + 넓은 여백 (§7 P0) |
| **타이포 인상** | 굵은 워드마크 + 충분한 여백. 장식보다 크기와 간격으로 존재감 확보 |

### 왜 A안인가 (재논의 방지용 기록)

```
North Star = 주간 재방문 참여 유저 수 → 읽기가 아니라 "쓰게 만드는 것"이 목적
정본       = "병원 정보 사이트가 아니라 커뮤니티다"

대안이었던 B안 「부드러운 전문성」(딥그린·정보성)은 매거진 SEO에는 유리하나
"정보 사이트"로 굳으면 참여가 일어나지 않는다. 정본이 명시적으로 경계한 지점이다.

A안은 정본 정합성이 높고, 인물 사진을 쓰지 않는 선택만으로도
우나어와 시각적으로 즉시 구분된다.
```

### 이름과 톤의 관계

"소란소란"은 의성어 반복이라 **밝고 가볍게** 읽힌다.
컬러와 여백을 차분하게 잡아야 이름의 가벼움과 균형이 맞는다.
이름이 이미 밝으므로 **색까지 밝으면 과잉이 된다.** 이것이 A안을 택한 실무적 이유이기도 하다.

---

## 4. 그대로 가져갈 것

> 브랜드 중립이며 재구축 가치가 없는 것. 우나어에서 검증된 자산이다.

| # | 자산 | 승계 이유 | 주의 |
|---|---|---|---|
| 1 | **Pretendard Variable** + dynamic subset | 한글 가독성 표준. 지연로드·CLS 방지 배선까지 완성 | OFL 라이선스 파일을 **신규 repo에 동봉**한다 (우나어에는 없다) |
| 2 | **본문 18px 기준** | 40~60대 타겟 검증값 | "기본" 단계의 본문 크기로 승계 (§8) |
| 3 | **글자 크기 3단계 기능** | 타겟에 필수 | 옵션·기본값·저장키는 전부 새로 정의 (§8) |
| 4 | **터치 타겟 52px 기준** | 오탭 방지 | 전역 하한 44px + 인터랙티브 52px 2단 구조 (§5) |
| 5 | **shadcn/ui + CSS Variables 구조** | `components.json` 설정 그대로 사용 가능 | `baseColor`는 재검토 |
| 6 | **Tailwind 토큰 매핑 방식** | `hsl(var(--token))` 패턴 | 값이 아니라 방식만 |
| 7 | **WCAG AA 대비 검증 습관** | 주석에 대비비를 남기는 규율 | 소란소란에도 강제 |
| 8 | **자체 SVG 아이콘 (일부)** | 브랜드 중립 아이콘 재사용 | 아래 표 참조 |
| 9 | **`next/og` ImageResponse 패턴** | 동적 OG 생성 구조 | 컬러·카피는 교체 (§5) |
| 10 | **`cn()` helper** (clsx + tailwind-merge) | 표준 | 그대로 |
| 11 | **서버 컴포넌트 우선 원칙** | `'use client'` 최소화 | 그대로 |
| 12 | **`--radius 0.75rem`** | 부드럽되 과하지 않음. A안 톤에 적합 | 그대로 |

### 8번 상세 — 아이콘 승계 범위

```
🟢 그대로 사용 (브랜드 중립)
   IconSearch · IconUser · IconBell · IconHeart · IconBookmark · IconShare
   IconFlag · IconEye · IconComment · IconLink · IconWrite · IconCopy · IconMore
   IconKakao (카카오 공식 마크 — 별도 가이드라인 준수)

🟡 IA 축소로 미사용 (남겨두되 배선하지 않음)
   IconStories · IconLife2 · IconEnergy · IconJobs

🟢 재배치
   IconBest    → 베스트(모아보기 영역)
   IconHeart   → 갱년기톡
   IconMagazine → 매거진
```

---

## 5. 수정해서 가져갈 것

> 구조는 유효하나 값·비율·네이밍을 반드시 바꿔야 하는 것.

| # | 자산 | 유지할 것 | 반드시 바꿀 것 |
|---|---|---|---|
| 1 | **컬러 토큰 3단 구조** | `--primary` / `--primary-text` / `--primary-strong` 3단 위계 | **값 전량 교체.** `#FF6F61` 계열 일절 사용 금지 |
| 2 | **`--primary-text` 대비 설계** | 개념 유지 | 우나어는 3.4:1로 **AA 경계**였다. 소란소란은 **처음부터 4.5:1 이상**으로 잡는다 |
| 3 | **`--primary-strong`** | 작은 글씨(배지·캡션)용 별도 토큰 | 새 컬러로 대비 재계산. **5.5:1 이상** |
| 4 | **`--muted-strong` / `--muted-subtle`** | 목록 3단 위계 (제목 > 미리보기 > 메타) | 웜 뉴트럴 계열로 재조정 |
| 5 | **갱년기 카테고리 컬러** | 전용 토큰을 두는 방식 | 🔴 우나어 핑크·마젠타 **폐기.** 갱년기톡은 소란소란의 **주인공**이므로 브랜드 컬러 계열로 승격 (§7 P1) |
| 6 | **IconMenu 구조** | 상단 아이콘 메뉴 행 + FAB | **7개 → 4개로 축소**: 갱년기톡 · 자유게시판 · 매거진 · 베스트 |
| 7 | **Header** | sticky 64px · 좌 로고 / 우 액션 | 🔴 **정사각 로고 슬롯 금지.** 가로형 워드마크 슬롯으로 (§9) |
| 8 | **동적 OG 템플릿** | `ImageResponse` 구조 · 카테고리 배지 + 제목 길이별 폰트 분기 | 그라디언트·배지 컬러·기본 카피 전부 교체 |
| 9 | **정적 OG 카피 톤** | "역할 말고 나 자신으로"라는 정서적 방향 | 문장은 소란소란 고유로 **재작성**. 우나어 문구 그대로 금지 |
| 10 | **manifest** | PWA 매니페스트 자체 | 🔴 `public/manifest.json` **복사 금지** → **`app/manifest.ts` 규약 권장** (타입 안전 + 값 누락 방지) |
| 11 | **metadata 생성 패턴** | `Metadata` 객체 구조 · `title.template` | `metadataBase` · verification · AdSense **전량 신규 값** |
| 12 | **localStorage 키** | 클라이언트 설정 저장 방식 | 🔴 `unao-*` **금지** → `soran-*` 네임스페이스 (§8) |
| 13 | **터치 타겟 2단 구조** | 전역 하한 + 개별 상향 | 관계를 **문서에 명시**한다: 전역 `min-height: 44px`(하한) · 인터랙티브 아이콘 버튼 **52px**(기준) |
| 14 | **코드 주석 명명** | 주석으로 의도를 남기는 습관 | 🔴 "시니어" 표현 **전량 치환** → "우리 나이" · "40~60대" · "가독성" |

---

## 6. 버릴 것

> 하나라도 넘어가면 소란소란이 우나어 복제품이 되거나 운영 사고가 난다.

| # | 폐기 대상 | 폐기 이유 | 위험도 |
|---|---|---|---|
| 1 | **`#FF6F61`** (우나어 코랄) | 복제품 인상. 네이버 이미지검색에서 두 서비스 혼동 | 🔴 |
| 2 | **우나어 로고 4종** (`logo.png` · `logo-symbol.png` · `images/logo.png` · `images/logo2.png`) | 우나어 워드마크 내장. 4종 중 2종은 내용이 같은 중복 파일 | 🔴 |
| 3 | **3원 겹침 심볼** | 우나어 고유 식별자 | 🔴 |
| 4 | **우나어 OG 이미지** (`og-cover.png` · `og-image.png`) | 로고·카피 내장. 두 파일은 내용 동일 | 🔴 |
| 5 | **AI 인물 사진 OG** | 우나어 브랜드에 귀속. 재사용 시 두 서비스에 같은 인물이 노출된다 | 🔴 |
| 6 | **favicon / icon / apple-touch-icon 전량** | 전부 우나어 마크 | 🔴 |
| 7 | **`age-doesnt-matter.com`** | 우나어 도메인 | 🔴 |
| 8 | **`img.age-doesnt-matter.com`** (preconnect) | 우나어 이미지 CDN. 복사 시 **불필요한 외부 커넥션 + 잘못된 도메인 신호** | 🟡 |
| 9 | **naver-site-verification · GSC · AdSense 기존 값** | 소유권 증명 값. 소란소란 신규 발급 필수 | 🔴 |
| 10 | **`com.agenotmatter.app`** (Play 스토어 연결) | 우나어 안드로이드 앱 | 🔴 |
| 11 | **`prefer_related_applications: true` 복사** | 🔴 **manifest를 무심코 복사하면 소란소란 PWA가 우나어 앱 설치를 유도한다.** 가장 눈에 안 띄면서 가장 치명적 | 🔴 |
| 12 | **`unao-*` localStorage 키** | 같은 브라우저에서 두 서비스가 설정을 공유·충돌한다 | 🔴 |
| 13 | **"시니어" 주석 / 명명** | 노출면에는 없으나 코드 주석에 남아 있다. 복사하면 그대로 옮겨간다 | 🟡 |
| 14 | **보라·핑크 gradient** (`--gradient-fame-*`) | 브랜드 원칙 3·4 위반 | 🔴 |
| 15 | **어드민 하드코딩 컬러** | 우나어 어드민은 토큰 미적용 구역이다. 복사하면 부채를 그대로 승계 | 🟡 |
| 16 | **미사용 잔재 자산** | 참조되지 않는 아이콘/이미지 파일. 이식 대상에서 제외 | 🟢 |

> ⚠️ **11번은 이번 감사에서 가장 위험한 항목이다.** 시각적으로 드러나지 않고, 테스트로도 잡히지 않으며,
> 사용자가 "앱 설치" 안내를 받았을 때 비로소 발견된다. manifest는 **복사하지 말고 새로 쓴다.**

---

## 7. 새로 만들 것 — P0 / P1 / P2

### 🔴 P0 — 없으면 D-day 배포 위험 (8종)

| # | 자산 | 요건 | 비고 |
|---|---|---|---|
| 1 | **primary color 1값** | 테라코타/브라운 계열. 흰 글씨 올릴 때 대비 확보 | 🔴 **나머지 P0 대부분이 여기 종속.** 최우선 결정 (§14) |
| 2 | **`--primary-text`** | 흰 배경 위 텍스트용. **4.5:1 이상** | 우나어 3.4:1 경계 문제를 반복하지 않는다 |
| 3 | **`--primary-strong`** | 작은 글씨용. **5.5:1 이상** | 배지·캡션·태그 |
| 4 | **텍스트 워드마크** | Pretendard Variable 800/900 | §9 조건 충족 시 이미지 파일 불요 |
| 5 | **Logo 컴포넌트 기준** | `src/components/brand/Logo.tsx` 단일 진입점 | 🔴 우나어가 놓친 지점. §9·§13 |
| 6 | **favicon 32 / 48 / 180** | 네이버가 검색결과에 favicon을 노출한다 | 없으면 백지 문서 아이콘 |
| 7 | **기본 OG 이미지 1200×630** | 인물 없이 타이포 중심 | 카톡 공유가 초기 유입 주경로. 없으면 빈 회색 카드 |
| 8 | **empty state 문구** | 게시판·검색·알림 각각 | 🔴 **실회원 글 0건으로 시작한다.** 빈 화면이 D-day 첫인상이다 |

> **`theme-color`는 1번이 정해지면 자동으로 따라온다** (primary와 동일값). 별도 결정 항목이 아니다.

#### P0 판정 기준

```
🟢 PASS   1~3번 확정 + 4~8번 확보 → D-day 진행
🟡 주의   6·7번 미확보 → D-day 가능하나 공유·검색 노출이 깨진다. D+3 내 보완
🔴 STOP   1번 미확정 → 첫 화면 구현이 시작되지 않는다. D-5 blocker로 승격
```

### 🟡 P1 — D+7 전 (5종)

```
1. secondary / accent 컬러
2. manifest icon 192 / 512 (maskable 포함)
3. 동적 OG 템플릿 (커뮤니티 글 · 매거진 글)
4. 갱년기톡 전용 컬러          ← 브랜드 컬러 계열로. 우나어 핑크 승계 금지
5. 기본 avatar / profile placeholder
```

### 🟢 P2 — D+30 전 (5종)

```
1. 매거진 OG 고도화 (시리즈·카테고리별 변형)
2. 매거진 썸네일 규칙 (비율 · 최소 해상도 · 대체 이미지)
3. Kakao 공유 이미지 최적화     ← 카톡 카드 비율은 OG와 별도 검증 필요
4. PWA screenshots              ← 우나어는 빈 배열 상태로 방치돼 있다
5. 브랜드 가이드 고도화 (톤앤매너 · 카피 라이팅 규칙)
```

---

## 8. 글자 크기 3단계 — 정본

> 🔴 **이 섹션이 이 문서에서 가장 자주 참조될 정본이다.** 구현 전 반드시 읽는다.

### 정본

| 항목 | 값 |
|---|---|
| **기능** | 소란소란에도 **유지한다** |
| **선택지** | **작게 / 기본 / 크게** — 3단계 |
| **기본값** | **기본** |
| **저장 시점** | 사용자가 선택하면 `localStorage`에 저장한다 |
| **저장키** | **`soran-font-size`** |
| **금지 키** | 🔴 `unao-font-size` · `unao-font-default-v2` — 우나어 키를 쓰지 않는다 |
| **금지 표현** | 🔴 "시니어 친화" · "시니어 맞춤" — 코드 주석·UI 문구 어디에도 쓰지 않는다 |

### 크기 기준

```
기본   본문 18px            ← 우나어에서 검증된 값을 "기본" 단계로 승계
작게   기본보다 한 단계 아래   ← 하한 16px. 그 아래로 내리지 않는다
크게   기본보다 한 단계 위
```

⚠️ **"작게"에도 하한이 있다.** 타겟 사용자층을 고려하면 16px 미만은 선택지로 제공하지 않는다.
사용자가 고를 수 있다는 것과, 읽을 수 없는 크기를 제공하는 것은 다르다.

### 구현 규칙

```
1. CSS 변수로 전 컴포넌트에 연동한다.
   개별 컴포넌트에 px를 하드코딩하면 3단계가 일부 화면에만 먹는다.

2. 저장키는 soran-font-size 단일 키로 설계한다.
   우나어는 승격 플래그(unao-font-default-v2)를 위해 키를 2개 썼으나,
   소란소란은 처음부터 기본값이 "기본"이므로 승격 로직 자체가 필요 없다.

3. localStorage 접근은 try/catch로 격리한다.
   Safari 프라이빗 모드 등에서 setItem이 던져도 화면 적용은 반드시 수행되어야 한다.

4. 초기 페인트 flicker를 막는다.
   설정 적용 스크립트를 <head>에서 동기 실행한다.

5. 어드민 화면은 이 설정의 영향을 받지 않게 격리한다.
```

### 🔴 변경 시 검증 필수 (3단계 전부에서 확인)

```
[ ] 게시글 목록      제목 2줄 말줄임 · 메타 정보 줄바꿈 깨짐 없음
[ ] 버튼             텍스트 넘침 없음 · 최소 터치 타겟 유지
[ ] 탭 / IconMenu    4개 항목이 한 줄에 유지되는가 (가로 스크롤 발생 금지)
[ ] 카드 레이아웃     썸네일 비율 유지 · 카드 높이 붕괴 없음
[ ] 모바일 767px     위 4개를 모바일 폭에서 재확인
```

⚠️ 우나어에서 이 검증이 문서화되어 있지 않았다. **"크게"에서만 깨지는 레이아웃은 개발자 화면에서 발견되지 않는다.**

---

## 9. 텍스트 워드마크 — D-day 조건

> 이미지 로고 없이 D-day 배포가 **가능하다.** 단 아래 6개를 모두 만족해야 한다.

| # | 조건 | 상세 |
|---|---|---|
| 1 | **폰트 고정** | Pretendard Variable **800 또는 900**. 얇은 굵기는 워드마크로서 존재감이 부족하다 |
| 2 | **4글자 반복 리듬 활용** | "소란소란"은 2음절 반복 구조다. 이 리듬 자체가 식별성을 만든다. 심볼 없이도 성립하는 이유 |
| 3 | **자간을 과하게 좁히지 않는다** | 한글은 자간을 좁히면 판독성이 급격히 떨어진다. 좁히더라도 **-2% 내외**까지 |
| 4 | **Logo 컴포넌트 단일 진입점** | 🔴 `src/components/brand/Logo.tsx` **1개소.** 페이지·컴포넌트에서 직접 마크업하지 않는다 |
| 5 | **Header는 가로형 슬롯** | 🔴 정사각 슬롯 금지. 우나어는 가로 2:1 로고를 정사각 박스에 넣어 **워드마크가 읽히지 않는 상태**로 운영했다 |
| 6 | **favicon은 별도 필요** | 32×32에서 한글 4글자는 판독 불가. 축약형(한 글자 또는 심볼)을 별도 설계한다 → P0-6 |

### 이미지 로고로의 무손실 전환

```
Logo.tsx 하나만 교체하면 전 화면에 반영된다.

우나어 반례:
  <Image src="/logo-symbol.png" ...> 가 6개 파일에 각각 하드코딩되어 있다.
  로고를 바꾸려면 6곳을 모두 찾아야 하고, 하나라도 빠지면 두 로고가 공존한다.

→ 소란소란은 D-day에 텍스트로 출발하더라도
  "나중에 교체 가능한 구조"를 처음부터 만든다. 이것이 4번 조건의 이유다.
```

---

## 10. 첫 화면 3초 브랜드 신호

> 우선순위 순 **4개.** 5개를 넘으면 3초 안에 읽히지 않는다.

| 순위 | 신호 | 목적 | 실패 사례 |
|---|---|---|---|
| **1** | **워드마크 "소란소란"** | 판독 가능한 크기로 | 우나어: 60×29로 축소되어 읽히지 않음 |
| **2** | **한 줄 정의 "40대 50대 여성이 이야기하는 곳"** | 🔴 **SEO title과 같은 문장을 화면에도 노출한다.** 검색결과와 첫 화면이 일치해야 신뢰가 생긴다 | 검색으로 들어왔는데 화면에 다른 말이 있으면 이탈 |
| **3** | **갱년기톡 최상단 · 핵심 노출** | 배치로 증명한다. "여긴 갱년기 얘기해도 되는 곳"이 카피 없이 전달된다 | 여러 게시판을 동등하게 나열하면 시작점이 사라진다 |
| **4** | **글쓰기 진입 (FAB)** | "읽는 곳"이 아니라 **"쓰는 곳"**임을 보여준다 | 읽기 전용으로 보이면 참여가 일어나지 않는다 |

### 🔴 의도적으로 넣지 않을 것

```
❌ 접속자 수 / "지금 N명이 보고 있어요"
❌ 실시간 활동 배지
❌ 총 게시글 수 / 회원 수
❌ 붉은 알림 도트 남발

이유: D-day에는 숫자가 작다. 작은 숫자를 노출하면 역효과이고,
      크게 보이게 꾸미면 브랜드 원칙 5(활발한 척 금지) 위반이다.
      숫자는 실제로 커진 뒤에 넣는다.
```

---

## 11. 매거진 / 커뮤니티 통일 원칙

> IA 정본(작전표 §4): **갱년기톡·자유게시판 = 게시판** / **매거진 = 콘텐츠 영역** / **베스트 = 모아보기 영역**

### 공유해야 하는 것 (같은 브랜드로 보이게)

```
1. 헤더 · 푸터 · 워드마크 동일
2. Pretendard + 본문 18px + 동일 타이포 스케일
3. radius · 그림자 · 터치 타겟 동일
4. primary 컬러 동일
```

### 구분해야 하는 것 (역할이 다름을 보이게)

```
5. 매거진   = 이미지 상단 카드 · 넓은 여백 · 읽기 리듬
   커뮤니티 = 텍스트 우선 리스트 · 높은 밀도 · 댓글 수 강조
```

### 🔴 금지

| 금지 | 이유 |
|---|---|
| 매거진과 커뮤니티에 **서로 다른 primary 컬러** | 두 개의 서비스처럼 보인다 |
| **매거진에 글쓰기 버튼 노출** | 매거진은 회원이 쓰는 곳이 아니다 |
| **커뮤니티에 "발행" · 운영자 용어 노출** | 회원이 자기 글을 "발행"한다고 느끼면 진입 장벽이 생긴다 |
| **매거진 글이 커뮤니티 리스트에 섞임** | IA 정본 위반. 데이터 모델에서 커뮤니티 여부를 분리해 구조적으로 막는다 |
| **베스트를 게시판처럼 표현** | 베스트는 모아보기 영역이다. 글쓰기 진입점을 두지 않는다 |

---

## 12. 금지 시각 요소

| 톤 | 구체적 금지 | 왜 위험한가 |
|---|---|---|
| 🚫 **병원 · 의료** | 민트/시안 + 화이트 클린톤 · 청진기·알약·체온계 아이콘 · "증상/치료/상담" 프레이밍 | 갱년기를 질병으로 규정한다. **커뮤니티가 아니게 된다** |
| 🚫 **건강기능식품** | 딥그린 + 골드 · 잎사귀 장식 · "효능" 뉘앙스 | 광고로 읽힌다. 신뢰가 아니라 경계심을 만든다 |
| 🚫 **복지관 · 기관** | 네이비+골드 · 딱딱한 세리프 · 큰 공지 배너 · 공공기관 느낌 | 타겟이 자신을 "복지 대상"으로 느끼게 한다 |
| 🚫 **과한 분홍 · 보라** | 마젠타·라벤더·핑크 그라디언트 | 브랜드 원칙 3·4 위반. 가장 흔한 클리셰 |
| 🚫 **젊은 SNS 감성** | 네온 그라디언트 · 글래스모피즘 · 과한 이모지 · 무한 스크롤 자극 · **하단 탭바** | 타겟과 어긋난다. 하단 탭바는 프로젝트 규칙상 금지 |
| 🚫 **우나어 복제 톤** | 코랄 `#FF6F61` · 3원 겹침 · 원형 오버랩 심볼 | 복제품 인상 |
| 🚫 **활발한 척** | 접속자 수 · 실시간 배지 · 알림 도트 남발 · 가짜 인기글 | 정본 위반. 실회원이 적을 때 즉시 들통난다 |
| 🚫 **AI 인물 사진 남용** | 스톡·AI 생성 인물을 반복 노출 | 광고처럼 보인다. **A안은 인물 없이 타이포로 간다** |

---

## 13. 구현 시 금지 사항

> 우나어 코드를 참고하는 것은 허용된다. **복사하는 것이 금지된다.**

| # | 금지 | 사고 시나리오 |
|---|---|---|
| 1 | **우나어 public asset 복사** | 로고·OG·favicon이 그대로 넘어가 복제품이 된다 |
| 2 | **우나어 manifest 복사** | 🔴 `prefer_related_applications: true` + 우나어 Play 앱 ID가 딸려와 **소란소란 PWA가 우나어 앱 설치를 유도한다** |
| 3 | **우나어 metadata verification 복사** | 소유권 증명 값이 어긋나 **검색엔진 등록이 실패**한다 |
| 4 | **우나어 localStorage 키 복사** | 같은 브라우저에서 두 서비스가 설정을 공유·충돌한다 |
| 5 | **토큰 밖 색상 리터럴 하드코딩** | 🔴 우나어는 토큰이 있는데도 코랄 리터럴이 39개 파일에 흩어져 있다. 컬러 변경이 **1줄 수정이 아니라 39파일 추적**이 된다 |
| 6 | **로고를 여러 파일에 직접 `<Image>`로 흩뿌리기** | 우나어는 6개 파일에 하드코딩되어 로고 교체가 사실상 불가능하다 |
| 7 | **"시니어" 표현을 주석째 복사** | 브랜드 금지어가 코드에 남는다 |
| 8 | **어드민 화면에 토큰 미적용** | 우나어 어드민은 하드코딩 구역이 되어 있다. **처음부터 토큰으로 시작한다** |

### 권장 안전장치

```
1. 색상 리터럴 린트 규칙을 처음부터 넣는다
   → globals.css 밖에서 #RRGGBB 사용 금지. 5번을 구조적으로 차단한다

2. Logo는 컴포넌트 1개소로 강제한다
   → 6번 차단

3. localStorage 키는 soran- 접두사 규약으로 통일한다
   → 4번 차단

4. manifest는 app/manifest.ts로 새로 작성한다 (복사 아님)
   → 2번 차단
```

---

## 14. 창업자 결정으로 남길 것 — 3개

> A안·텍스트 워드마크·타이포 OG는 **확정됐다.** 다시 묻지 않는다.

| # | 결정 항목 | 기한 | 왜 창업자 결정인가 |
|---|---|---|---|
| **1** | **primary color 최종 1값** | 🔴 D-7 이전 | P0 8종 중 4종이 여기 종속. 방향(테라코타/브라운)은 정해졌으나 최종 1값은 취향·상표 인상의 영역이다 |
| **2** | **보드별 첫 글 / empty state 문구 톤** | D-5 | 커뮤니티의 첫인상을 만드는 목소리다. 브랜드 오너의 언어여야 한다 |
| **3** | **D-day 날짜와 연결된 브랜드 자산 마감일** | 🔴 D-7 이전 | 브랜드 자산은 유일하게 리드타임이 있다. D-day가 정해져야 역산이 가능하다 |

### 1번을 좁히기 위한 제약 (결정을 돕는 정보)

```
계열     따뜻한 뉴트럴 기반 + 차분한 테라코타/브라운 포인트
제외     #FF6F61 및 인접 코랄 · 핑크 · 보라 · 민트/시안 · 형광

대비 요건 (이 3개를 동시에 만족해야 한다)
  버튼 fill 위 흰 글씨     4.5:1 이상
  흰 배경 위 텍스트         4.5:1 이상   ← 우나어는 3.4:1로 경계였다
  작은 글씨(배지·캡션)      5.5:1 이상

→ 값이 정해지면 3단 토큰과 theme-color가 자동으로 따라 결정된다.
```

---

## 15. 다음 작업 (Claude)

> 구현 지시가 아니다. **순서와 선행 조건**만 정의한다.

| 순서 | 작업 | 선행 조건 |
|---|---|---|
| 1 | 상위 문서 정합성 정정 — 작전표 §2 체크리스트, m3 §13 11축 공백 | 이 문서 확정 |
| 2 | 컬러 3값 대비 계산표 작성 | §14-1 결정 |
| 3 | OG 카피 후보 작성 | §14-1·§14-2 결정 |
| 4 | empty state 문구 후보 작성 | §14-2 결정 |
| 5 | 토큰 파일 골격 · `Logo.tsx` 인터페이스 설계 | 🔴 **신규 repo 생성 이후에만 가능** (작전표 §1 B1) |
| 6 | 색상 리터럴 린트 규칙 설계 | 5번과 동시 |

⚠️ **5·6번은 `MogoKim/soransoran` repo가 생기기 전에는 착수할 수 없다.**
작전표 §1의 B1 blocker가 이 문서의 실행 병목이기도 하다.

---

## 16. 이 문서의 판정 기준

```
이 문서는 "무엇을 만들지"가 아니라 "무엇을 하면 안 되는지"를 고정한다.

브랜드 자산 제작·구현 과정에서 판단이 갈리면:
  1. §2 브랜드 원칙 7개를 먼저 본다
  2. §12 금지 시각 요소에 걸리는지 본다
  3. §13 구현 금지 사항에 걸리는지 본다
  4. 그래도 갈리면 창업자에게 올린다 — 임의로 정하지 않는다
```

**가장 중요한 한 문장**

> 처음 들어온 사람이 3초 안에
> **여기가 40대 50대 여성이 자기 이야기를 할 수 있는 갱년기 중심 커뮤니티**라는 것을 알아야 한다.
