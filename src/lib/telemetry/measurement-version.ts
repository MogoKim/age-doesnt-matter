/**
 * 계측 계약 버전 — **경계는 달력 시각이 아니라 이벤트에 실린 버전이다.**
 *
 * ## 왜 배포 시각 상수를 두지 않는가
 * 배포 시각을 코드 상수로 기록하려면 계측 배포가 끝난 뒤 **또 한 번 PR·재배포**를 해야 한다.
 * 게다가 그 시각으로 갈라도 틀린다 — 배포 직후에도 **캐시된 구버전 클라이언트**가 한동안
 * 미버전 이벤트를 계속 보내기 때문이다. 달력으로 자르면 그 이벤트들이 신규 집계에 섞인다.
 *
 * 그래서 분리 기준은 **이벤트가 이 버전을 달고 왔는지** 하나뿐이다(보수적 분리).
 * "언제부터 v2 가 들어오기 시작했나"가 필요하면 **실제 최초 관측 시각**을 집계에서 읽는다
 * (`bannerCtaV2.firstSeenInWindowAt` 등). 운영 기록이 필요하면 운영 문서에만 남긴다.
 */
export const MEASUREMENT_VERSION = 'r8-v2'
export type MeasurementVersion = typeof MEASUREMENT_VERSION
